# Tag版本哈希型缓存机制完整设计文档
> 逻辑失效派 · 企业级两级缓存适配方案

本文档系统梳理 Tag 版本哈希型缓存机制的核心原理、工作流程、工程落地与架构适配，专为「本地内存 + Redis」两级缓存架构设计，全程围绕「逻辑失效」核心思想展开。

---

## 一、核心本质：什么是逻辑失效
### 1.1 核心定义
不主动删除任何旧缓存键，只修改标签对应的版本元数据，让缓存键的计算结果发生变化，旧缓存键因「键名不再匹配」而无人访问，最终依靠 TTL 自然淘汰，实现**逻辑上的失效**。

一句话概括：换个编号规则，旧编号的东西就等于不存在了。

### 1.2 与物理删除的本质区别
| 维度 | 物理删除（Set反向映射方案） | 逻辑失效（版本哈希方案） |
|------|------------------------------|--------------------------|
| 失效动作 | 遍历标签下所有缓存键，逐条删除 | 仅修改标签对应的版本号，不碰任何缓存键 |
| 失效效果 | 缓存键立即从存储中消失 | 旧缓存键仍存在，但后续读取不会再命中 |
| 时间复杂度 | O(N)，N 为标签关联的缓存键数量 | O(1)，与关联缓存键数量无关 |
| 内存回收 | 失效即释放内存 | 依赖 TTL 到期后自动回收 |
| 两级缓存适配 | 需要额外引入消息广播同步本地缓存 | 天然适配，无需额外机制 |

### 1.3 设计哲学
用极短期的少量内存冗余，换取极高的失效性能、极强的架构兼容性、极低的实现复杂度。
对于绝大多数业务场景，缓存内存占用不是瓶颈，高并发下缓存失效导致的 Redis 阻塞、多级缓存不一致、竞态脏数据才是真正的生产事故来源，逻辑失效正是在这个权衡下的最优解。

---

## 二、从单标签起步：你已经在用的版本号机制
你当前项目中的 `resource + version` 机制，本质就是**单标签版本机制**，是 Tag 方案的最简形态。

### 2.1 基础逻辑
- 给每一类资源（如 `role`）分配一个版本号
- 版本号直接嵌入缓存键，格式如 `global:v1:role:123`
- 资源数据变更时，递增该资源的版本号（如从 1 变 2）
- 版本号变化后，缓存键变成 `global:v2:role:123`，旧键自动无人访问

### 2.2 局限
只能按「资源大类」整体失效，粒度太粗。比如修改任意一个角色，所有角色的缓存都会全部失效，无法做到只失效被修改的那一条数据，缓存命中率低。

---

## 三、多标签扩展：从一维到多维
### 3.1 为什么需要多标签
业务数据的依赖往往是多维度的。比如「用户的权限数据」，同时依赖用户自身、所属角色、租户等多个维度，任何一个维度变化，缓存都应该失效。
单标签只能管一个维度，多标签可以同时承载多个依赖维度，实现精准的联动失效。

### 3.2 多标签的工作方式
给一份缓存打上它依赖的所有标签，缓存键由所有标签的当前版本共同决定。任何一个标签的版本变化，都会导致最终缓存键变化，触发失效。

举个例子：
用户1同时属于角色1、角色2，租户ID为1001，则他的权限缓存同时打三个标签：`tenant:1001`、`role:1`、`role:2`。
- 修改角色1 → `role:1` 版本变化 → 缓存失效
- 修改角色2 → `role:2` 版本变化 → 缓存失效
- 租户注销 → `tenant:1001` 版本变化 → 缓存失效

### 3.3 哈希的作用：为什么要做哈希压缩
如果直接把所有标签的版本号拼进缓存键，标签多了会导致键很长，浪费内存、降低查询性能，且标签顺序不一致会生成不同的键，容易出 Bug。
哈希就是一个「压缩工具」：
1. 将所有标签+版本号按固定顺序拼接成字符串
2. 通过哈希算法计算出固定长度的指纹（哈希值）
3. 用哈希值替代长串嵌入缓存键

**核心特性**：
- 相同的标签+版本，永远算出同一个哈希
- 只要有一个标签版本变化，哈希结果就完全不同
- 无论多少个标签，哈希值长度固定

> 注意：哈希只是优化手段，不是核心逻辑。即使不用哈希，直接拼接版本号，机制也能正常工作。

---

## 四、典型场景全链路推演
以「用户角色权限」场景为例，用具体数值完整走一遍读写+失效全流程。

### 前提约定
- 用户1 拥有 角色1
- 角色1 当前有「查看订单」权限
- 标签 `role:1` 当前版本号 = 5
- 缓存 TTL = 2小时

### 第一步：用户1登录，写入权限缓存
1. **识别依赖标签**：用户权限由角色决定，打上标签 `role:1`
2. **读取标签版本**：从 Redis 中查询 `role:1` 的版本 → 5
3. **计算哈希**：将 `role:1=5` 计算哈希，得到 `a7f2d9`
4. **生成最终键**：`data:a7f2d9:user_perm:1`
5. **写入两级缓存**：将权限数据同时写入 L1 本地内存和 L2 Redis

> 关键：缓存键里嵌入了角色的当前版本，版本不变则键不变，版本变则键必变。

### 第二步：管理员修改角色1的权限，触发失效
1. 数据库更新角色1的权限，新增「删除订单」权限
2. 执行一条 Redis 原子命令：`INCR cache_tag:version:role:1`
3. `role:1` 的版本号从 5 变为 6
4. 操作结束，不删除任何缓存键

不需要查询哪些用户拥有角色1，也不需要遍历删除用户缓存，仅修改一个数字。

### 第三步：用户1后续访问，自动获取最新权限
1. **重新读取标签版本**：查询 `role:1` 版本 → 已变为 6
2. **重新计算哈希**：`role:1=6` 算出新哈希 `b3z8q4`
3. **生成新缓存键**：`data:b3z8q4:user_perm:1`
4. **读取缓存未命中**：缓存中只有旧键 `data:a7f2d9:user_perm:1`
5. **回源数据库**：查询用户1的最新权限，拿到包含「删除订单」的最新数据
6. **写入新缓存**：用新哈希写入最新数据

结果：用户无需重新登录，自动获取最新权限。旧缓存键保留至 TTL 到期后自动删除。

### 第四步：新用户登录，直接拿到最新数据
用户2也拥有角色1，此时首次登录：
1. 读取 `role:1` 版本 → 6
2. 计算哈希生成缓存键
3. 缓存未命中，回源数据库
4. 拿到修改后的最新权限，写入缓存

结果：新用户登录即获取最新数据，绝对不会读到旧缓存。

### 扩展：多角色场景
如果用户同时拥有角色1和角色2，缓存同时打上 `role:1`、`role:2` 两个标签：
- 任意一个角色版本变化，哈希都会变化，缓存都会失效
- 未被修改的角色不影响缓存命中，命中率更高

---

## 五、工程落地核心细节
### 5.1 标签版本的存储设计
#### 存储位置
- **权威源：Redis（L2）**：所有服务实例共享同一份版本数据，保证分布式环境下一致性。绝对不能只存在本地内存，否则多节点版本不一致。
- **可选优化：本地短时缓存**：可在本地内存缓存 1~3 秒，减少 Redis 查询次数，代价是最多有 1~3 秒的失效延迟，绝大多数业务可接受。

#### 物理结构
每个标签对应一个独立的 Redis String 键，不是一个大字典/哈希表。

| Redis 键名格式 | 键值类型 | 操作命令 |
|----------------|----------|----------|
| `cache_tag:version:{标签名}` | 整数（从0开始递增） | `GET` 读取，`INCR` 原子递增 |

示例：
```
cache_tag:version:role:1       → 6
cache_tag:version:role:2       → 3
cache_tag:version:tenant:1001  → 2
```

#### 为什么不用一个大字典存储
1. **并发不安全**：多请求同时修改不同标签，会出现覆盖写，丢失版本更新
2. **性能差**：每次只改一个标签，也要读写整个大字典，无意义开销大
3. **原子性差**：`INCR` 是 Redis 原生原子命令，单键操作天然并发安全

### 5.2 标签粒度选择
没有强制规定，完全按需选择，工业界标准做法是**粗细搭配混合打标**。

| 粒度 | 命名示例 | 失效范围 | 适用场景 |
|------|----------|----------|----------|
| 表级（粗粒度） | `role:all`、`perm:all` | 一整张表的缓存全部失效 | 全表批量更新、数据字典刷新、兜底清理 |
| 行级（细粒度） | `role:1`、`user:123` | 只失效关联该行的缓存 | 单条记录修改，精准失效，命中率高 |

#### 最佳实践：三层标签模型
一份缓存同时打多层标签，兼顾灵活度和命中率：
1. **租户层**：`tenant:{id}`，强制携带，实现租户级隔离与批量清理
2. **实体层**：`{entity}:{id}`，单实体变更时精准失效
3. **业务层**：`biz:{name}`，业务维度批量失效

### 5.3 版本号生命周期
- **TTL 设置**：标签版本键的 TTL 必须长于最长的业务缓存 TTL。比如缓存最长 6 小时，版本键设 24~72 小时，防止版本键先过期重置为 0，导致旧缓存被重新命中。
- **溢出问题**：Redis 整数为 64 位，每秒递增一次可运行几十亿年，工程上无需考虑溢出。
- **全局兜底**：可设置一个 `system:global` 全局标签，所有缓存都携带。线上出现大面积缓存异常时，只需递增该标签，即可一键失效全量缓存，替代 `FLUSHALL`。

### 5.4 两级缓存的天然一致性
这是版本哈希方案最核心的优势之一：
- L1 和 L2 使用完全相同的缓存键
- 标签版本变化后，键名同步变化，两层缓存中的旧键同时逻辑失效
- 无需额外的消息广播、无需遍历本地缓存、无复杂的同步逻辑
- 唯一不一致窗口：L1 中已存在的旧键会在 L1 的 TTL 到期后淘汰，L1 本身通常为短 TTL（如 40 秒），窗口完全可控

---

## 六、统一缓存管理器的架构设计
### 6.1 内部分层：控制平面 + 数据平面
标签版本号属于**缓存元数据（控制平面）**，业务缓存属于**业务数据（数据平面）**，二者在缓存管理器内部分层管理，对外暴露统一接口，不存在「自己管自己」的问题。

| 分层 | 定位 | 存储策略 | 作用 |
|------|------|----------|------|
| 元数据层（控制平面） | 缓存系统自身的规则数据 | 只走 Redis（权威源），本地可短时缓存 | 决定缓存键格式、判断缓存有效性 |
| 数据层（业务平面） | 实际业务缓存数据 | L1 内存 + L2 Redis 两级缓存，dogpile 统一管理 | 存放业务数据，提供读写能力 |

类比：缓存管理器是图书馆管理员，业务缓存是书架上的书，标签版本号是管理员手里的图书编号规则。规则由管理员保管，用来管理图书，不是自己管理自己。

### 6.2 为什么不会破坏统一管理
- **对内分层，对外统一**：业务层只调用统一的读写/失效接口，完全不知道底层分层实现，所有逻辑收口在缓存管理器内部
- **键计算完全收口**：最终缓存键的生成全部在缓存管理器内部完成，业务层只传业务 key 和标签列表，不会出现多方计算不一致的问题
- **两级缓存逻辑不变**：业务数据的读写依然完全由 dogpile 管理，L1/L2 同步、防击穿、锁机制全部保留

### 6.3 参考实现骨架
```python
import time
import mmh3
import redis
from dogpile.cache import make_region

class CacheManager:
    def __init__(self, redis_url: str):
        # 原生Redis客户端：专门管理标签版本元数据
        self.redis_client = redis.from_url(redis_url)
        # dogpile两级缓存：专门管理业务缓存数据
        self.cache_region = make_region().configure(
            "dogpile.cache.redis",
            arguments={"url": redis_url},
            expiration_time=21600
        )
        # 本地短时缓存标签版本，减少Redis查询
        self._local_version_cache = {}
        self._version_cache_ttl = 1  # 本地缓存1秒

    # --------------------------
    # 内部私有方法：标签版本管理
    # --------------------------
    def _get_tag_version(self, tag: str) -> int:
        """获取单个标签的当前版本，内部使用"""
        now = time.time()
        # 先查本地短时缓存
        if tag in self._local_version_cache:
            ver, expire = self._local_version_cache[tag]
            if now < expire:
                return ver
        # 读取Redis权威值
        raw = self.redis_client.get(f"cache_tag:version:{tag}")
        version = int(raw) if raw else 0
        # 更新本地缓存
        self._local_version_cache[tag] = (version, now + self._version_cache_ttl)
        return version

    def _increment_tag_version(self, tag: str) -> int:
        """原子递增标签版本，内部使用"""
        new_version = self.redis_client.incr(f"cache_tag:version:{tag}")
        # 失效本地缓存，下次读取强制拉取最新值
        self._local_version_cache.pop(tag, None)
        return new_version

    def _calc_tag_hash(self, tags: list[str]) -> str:
        """根据标签列表计算当前版本哈希"""
        # 按字典序排序，保证相同标签组合结果一致
        sorted_tags = sorted(tags)
        version_parts = [f"{t}={self._get_tag_version(t)}" for t in sorted_tags]
        version_str = "|".join(version_parts)
        # 128位哈希，转十六进制字符串
        return mmh3.hash128(version_str, signed=False).hex()

    # --------------------------
    # 对外统一接口：业务层调用
    # --------------------------
    def get(self, base_key: str, tags: list[str] = None):
        """读取缓存"""
        tags = tags or []
        tag_hash = self._calc_tag_hash(tags)
        final_key = f"{tag_hash}:{base_key}"
        return self.cache_region.get(final_key)

    def set(self, base_key: str, value, tags: list[str] = None, ttl: int = None):
        """写入缓存"""
        tags = tags or []
        tag_hash = self._calc_tag_hash(tags)
        final_key = f"{tag_hash}:{base_key}"
        self.cache_region.set(final_key, value, expiration_time=ttl)

    def invalidate_tags(self, tags: list[str]):
        """失效指定标签"""
        for tag in tags:
            self._increment_tag_version(tag)
```

---

## 七、优势、边界与项目演进
### 7.1 核心优势
1. **失效性能极高**：O(1) 复杂度，无论标签下关联多少缓存键，失效开销完全相同，高并发下无 Redis 阻塞风险
2. **两级缓存天然友好**：L1+L2 无需额外同步机制，是两级缓存架构的最优解
3. **实现极简可靠**：核心逻辑简单，无集合维护、消息广播等复杂逻辑，故障概率低
4. **无并发竞态**：不删除缓存，彻底避免「删缓存与写缓存并发」导致的脏数据问题
5. **降级友好**：版本键故障时可降级为默认版本 0，最多缓存不失效，不会导致服务不可用

### 7.2 局限性与适用边界
1. **内存非即时释放**：旧缓存键依赖 TTL 淘汰，更新极频繁的热点数据会产生较多过期键。应对：缩短 TTL、配合 Redis 惰性删除。
2. **存在不一致窗口**：L1 短 TTL 窗口内可能读到旧数据。应对：缩短 L1 TTL、增加轻量 Pub/Sub 提前淘汰。
3. **不适合超大值缓存**：单条缓存几百 MB、更新后希望立即释放内存的场景不适用，普通业务数据无压力。

### 7.3 适配你项目的演进步骤
你当前的 `resource 版本号` 就是单标签版本机制，升级为多标签属于平滑演进，无需推倒重来。建议分三步落地：

1. **底层能力扩展**
   在 `CacheManager` 中新增多标签相关方法，保留原有单 resource 版本逻辑，两套机制并行，保证业务兼容。

2. **单模块试点**
   选择 RBAC 权限模块作为试点，迁移到标签机制，删除重复的 ORM 事件监听，验证失效准确性与命中率。

3. **全量替换与清理**
   将 ORM 全局事件改为实体级标签失效，逐步迁移所有业务模块，最终移除旧的 resource 版本逻辑，简化架构。

---

## 总结
Tag 版本哈希机制的核心，是将「删除缓存数据」转化为「更新版本规则」，用极小的内存代价，换取了高性能、高可靠、天然适配多级缓存的缓存失效方案。对于你的多租户 SaaS 项目，它既能解决当前缓存失效粒度粗、两套事件冲突的问题，也能天然支撑租户隔离、级联失效等企业级需求，是非常契合的技术路线。