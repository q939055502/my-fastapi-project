# 缓存系统重构方案

## 一、现状分析

### 1.1 当前依赖版本

| 依赖 | 当前版本 | 用途 |
|------|---------|------|
| SQLAlchemy | 2.0.49 | ORM框架，已使用新语法 `select()` |
| dogpile.cache | 1.5.0 | 缓存框架（已安装但未使用） |
| redis | latest | Redis客户端（原生使用） |

### 1.2 严重问题：租户隔离完全失效

**核心问题**：项目全部使用 SQLAlchemy 2.0 新语法 `session.execute(select(...))`，但租户隔离事件监听的是旧版 `Query` 对象：

```python
# tenant_isolation.py - 完全无效！
from sqlalchemy.orm import Query
@event.listens_for(Query, 'before_compile', retval=True)  # ❌ 新语法不会触发
```

**影响**：37 个文件使用 `session.execute(select(...))`，全部无法被租户隔离拦截，存在严重安全隐患。

**修复方案**：改用 `Session.do_orm_execute` 事件 + `with_loader_criteria()`：

```python
# tenant_isolation.py - 正确写法（SQLAlchemy 2.0）
from sqlalchemy import event
from sqlalchemy.orm import with_loader_criteria

@event.listens_for(Session, 'do_orm_execute')
def apply_tenant_isolation(session, orm_execute_state):
    if (orm_execute_state.is_select and
        not orm_execute_state.is_column_load and
        not orm_execute_state.is_relationship_load):
        orm_execute_state.statement = orm_execute_state.statement.options(
            with_loader_criteria(...)
        )
```

### 1.2 当前缓存架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        CacheManager                             │
│  ┌──────────────┐    读穿透     ┌──────────────┐               │
│  │  L1LocalCache│ ────────────→ │   L2RedisCache│             │
│  │ (OrderedDict │               │  (redis-py)   │             │
│  │   LRU)       │ ←──────────── │               │               │
│  └──────────────┘    写穿透     └──────────────┘               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   SQLAlchemy Event 监听                         │
│  @event.listens_for(Session, 'after_flush')                    │
│  写操作后自动清除相关缓存                                         │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 当前文件结构

```
src/core/storage/cache/
├── __init__.py           # 模块导出
├── cache_key.py          # 缓存键构建器
├── cache_manager.py      # 统一缓存管理器（L1+L2）
├── l1_local.py           # 原生 OrderedDict LRU 实现
└── l2_redis.py           # 原生 redis-py 实现
```

### 1.4 当前缓存使用场景

| 使用位置 | 使用方式 | 说明 |
|---------|---------|------|
| `cache_events.py` | `cache_manager._clear_pattern_raw()` | ORM事件触发缓存清除 |
| `cache_warmup_initializer.py` | `cache_manager.set_global()` | 启动时缓存预热 |
| `login_ctx.py` | `cache_manager.get/set/delete()` | 登录上下文缓存 |
| `permission_control.py` | `cache_manager.get()` | 权限校验缓存 |
| `tenant/resolver.py` | `cache_manager.get()` | 租户解析缓存 |
| `user_admin_service.py` | `cache_manager.set()` | 用户管理缓存 |
| `uuid_resolver.py` | `cache_manager.get/set()` | UUID解析缓存 |

---

## 二、重构目标

### 2.1 核心目标

1. **使用 dogpile.cache 框架替换原生缓存实现**
   - L1: 使用 `dogpile.cache.memory` 替换自定义 `OrderedDict` LRU
   - L2: 使用 `dogpile.cache.redis` 替换自定义 `redis-py` 封装

2. **利用 dogpile.cache 的读写锁机制**
   - 解决缓存击穿问题（多个线程同时缓存失效时只触发一次数据库查询）

3. **保持现有缓存键设计和版本号机制**
   - 保留 `cache_key.py` 的缓存键分层设计
   - 保留版本号失效策略

4. **增强 SQLAlchemy 事件监听的缓存联动**
   - 不仅清除缓存，还支持写入缓存（读操作时自动缓存）

### 2.2 设计原则

- **不兼容旧代码**：全量改造，移除原生缓存实现
- **单向依赖**：`src/core/` 不依赖业务逻辑
- **可序列化约束**：缓存值必须是可 JSON 序列化的纯数据结构，禁止缓存 ORM 对象

---

## 三、重构方案

### 3.1 新增依赖

需添加 `dogpile.cache.redis` 后端支持：

```toml
# pyproject.toml
dogpile-cache-redis = "^1.1"
```

### 3.2 文件结构变更

```
src/core/storage/cache/
├── __init__.py               # 模块导出（更新）
├── cache_key.py              # 缓存键构建器（保留）
├── cache_manager.py          # 统一缓存管理器（重写）
├── dogpile_config.py         # dogpile.cache 配置（新增）
├── orm_cache_events.py       # ORM事件缓存联动（新增，替代cache_events.py）
└── utils.py                  # 工具函数（新增）
```

**删除文件**：
- `l1_local.py` - 原生 L1 缓存实现
- `l2_redis.py` - 原生 L2 缓存实现

### 3.3 核心模块设计

#### 3.3.1 `dogpile_config.py` - dogpile.cache 配置

```python
# 配置两个缓存后端：memory（L1）和 redis（L2）
# 支持命名缓存区域（namespace）
# 配置序列化器（JSON）
```

#### 3.3.2 `cache_manager.py` - 缓存管理器（重写）

```python
# 封装 dogpile.cache 的 CacheRegion
# 提供统一的 get/set/delete/clear 接口
# 保留版本号机制（通过键前缀实现）
# 提供装饰器模式的缓存接口
```

#### 3.3.3 `orm_cache_events.py` - ORM 事件缓存联动

```python
# 监听 SQLAlchemy Session 事件：
# - after_flush: 写操作后清除相关缓存
# - after_load: 读操作后写入缓存（可选）
# 按模型名映射缓存资源名
```

### 3.4 缓存键设计（保留优化）

保持现有分层设计：

```
GLOBAL: global:{version}:{resource}:{key}
DATA:   data:{version}:{tenant_id}:{resource}:{key}
LIST:   list:{version}:{tenant_id}:{resource}:{query_hash}
```

**优化点**：
- 版本号通过键前缀实现，递增后旧键自然过期
- tenant_id 用于租户隔离

### 3.5 dogpile.cache 配置详情

```python
# dogpile_config.py

from dogpile.cache import make_region
from dogpile.cache.backends.memory import MemoryBackend
from dogpile.cache.backends.redis import RedisBackend
import json

# L1 本地内存缓存
l1_region = make_region().configure(
    'dogpile.cache.memory',
    expiration_time=60,  # 默认TTL
    arguments={
        'max_entries': 1000,  # 最大条目数
    }
)

# L2 Redis 分布式缓存
l2_region = make_region().configure(
    'dogpile.cache.redis',
    expiration_time=43200,  # 默认TTL 12小时
    arguments={
        'url': settings.REDIS_URL,
        'redis_expiration_time': 43200,
        'distributed_lock': True,  # 启用分布式锁
    },
    serializer={
        'load': json.loads,
        'dump': json.dumps,
    }
)

# 二级缓存组合（读穿透）
def get_cached(key):
    value = l1_region.get(key)
    if value is None:
        value = l2_region.get(key)
        if value is not None:
            l1_region.set(key, value)
    return value

def set_cached(key, value, ttl=None):
    l1_region.set(key, value, expiration_time=ttl)
    l2_region.set(key, value, expiration_time=ttl)

def delete_cached(key):
    l1_region.delete(key)
    l2_region.delete(key)
```

### 3.6 ORM 事件监听设计

```python
# orm_cache_events.py

from sqlalchemy import event
from sqlalchemy.orm import Session

from src.core.storage.cache import cache_manager

_CACHE_RESOURCE_MAP = {
    "Role": "role",
    "Permission": "permission",
    "RolePermission": "role_permission",
    "RoleSubject": "role_subject",
    "DataScopeRule": "data_scope_rule",
    "User": "user",
    "Member": "member",
    "Org": "org",
    "OrgSubject": "org_subject",
    "DictType": "dict_type",
    "DictData": "dict_data",
}

@event.listens_for(Session, 'after_flush')
def after_flush_cache(session, flush_context):
    """写操作后自动清除缓存"""
    affected_resources = set()
    
    for instance in list(session.new) + list(session.dirty) + list(session.deleted):
        model_name = type(instance).__name__
        resource = _CACHE_RESOURCE_MAP.get(model_name)
        if resource:
            affected_resources.add(resource)
    
    for resource in affected_resources:
        cache_manager.increment_version(namespace=resource)

@event.listens_for(Session, 'after_commit')
def after_commit_cache(session):
    """事务提交后清理脏缓存"""
    cache_manager.cleanup()
```

---

## 四、迁移步骤

### 阶段一：新增 dogpile.cache 配置层

| 步骤 | 操作 | 文件 | 说明 |
|------|------|------|------|
| 1 | 创建配置文件 | `dogpile_config.py` | 配置 L1/L2 缓存后端 |
| 2 | 创建工具函数 | `utils.py` | 提供二级缓存读写封装 |
| 3 | 更新模块导出 | `__init__.py` | 导出新的缓存管理器 |

### 阶段二：重写缓存管理器

| 步骤 | 操作 | 文件 | 说明 |
|------|------|------|------|
| 1 | 重写 CacheManager | `cache_manager.py` | 使用 dogpile.cache 替代原生实现 |
| 2 | 保留版本号机制 | `cache_manager.py` | 通过键前缀实现版本管理 |
| 3 | 保留装饰器接口 | `cache_manager.py` | `@cache_manager.cached()` |

### 阶段三：重写 ORM 事件监听

| 步骤 | 操作 | 文件 | 说明 |
|------|------|------|------|
| 1 | 创建新事件文件 | `orm_cache_events.py` | 重写缓存联动逻辑 |
| 2 | 删除旧事件文件 | `cache_events.py` | 移除旧实现 |

### 阶段四：删除旧缓存实现

| 步骤 | 操作 | 文件 | 说明 |
|------|------|------|------|
| 1 | 删除 L1 实现 | `l1_local.py` | 已被 dogpile.cache.memory 替代 |
| 2 | 删除 L2 实现 | `l2_redis.py` | 已被 dogpile.cache.redis 替代 |

### 阶段五：更新调用方

| 步骤 | 操作 | 文件 | 说明 |
|------|------|------|------|
| 1 | 更新缓存预热 | `cache_warmup_initializer.py` | 使用新的 `cache_manager` |
| 2 | 更新登录上下文 | `login_ctx.py` | 使用新接口 |
| 3 | 更新权限控制 | `permission_control.py` | 使用新接口 |
| 4 | 更新租户解析 | `tenant/resolver.py` | 使用新接口 |
| 5 | 更新用户服务 | `user_admin_service.py` | 使用新接口 |
| 6 | 更新 UUID 解析 | `uuid_resolver.py` | 使用新接口 |

### 阶段六：修复租户隔离（关键安全修复）

| 步骤 | 操作 | 文件 | 说明 |
|------|------|------|------|
| 1 | 重写租户隔离 | `tenant_isolation.py` | 改用 `Session.do_orm_execute` + `with_loader_criteria()` |
| 2 | 删除旧代码 | `tenant_isolation.py` | 移除 `Query.before_compile` 和 `query.column_descriptions` |
| 3 | 验证测试 | - | 验证 SELECT/UPDATE/DELETE 都能被正确拦截 |

**重写后的核心逻辑**：

```python
# tenant_isolation.py（SQLAlchemy 2.0 正确写法）

from sqlalchemy import event
from sqlalchemy.orm import Session, with_loader_criteria

@event.listens_for(Session, 'do_orm_execute')
def apply_tenant_isolation(session, orm_execute_state):
    tenant_scope = get_tenant_scope_for_read()
    
    if tenant_scope.skip or tenant_scope.filter_mode == 'skip':
        return
    
    if orm_execute_state.is_select and not orm_execute_state.is_column_load:
        if tenant_scope.filter_mode == 'eq':
            orm_execute_state.statement = orm_execute_state.statement.options(
                with_loader_criteria(
                    lambda cls: hasattr(cls, 'tenant_id'),
                    lambda cls: cls.tenant_id == tenant_scope.filter_value
                )
            )
        elif tenant_scope.filter_mode == 'is_null':
            orm_execute_state.statement = orm_execute_state.statement.options(
                with_loader_criteria(
                    lambda cls: hasattr(cls, 'tenant_id'),
                    lambda cls: cls.tenant_id.is_(None)
                )
            )
```

---

## 五、关键注意事项

### 5.1 序列化约束

**禁止缓存 ORM 对象**，必须缓存纯数据结构（dict/list/str/int）：

```python
# ❌ 错误：缓存 ORM 对象（会导致序列化失败）
cache_manager.set("user", str(user.id), user)

# ✅ 正确：缓存可序列化的字典
cache_manager.set("user", str(user.id), {
    "id": user.id,
    "username": user.username,
    "email": user.email,
})
```

### 5.2 分布式锁配置

dogpile.cache 的 Redis 后端默认启用分布式锁，可有效防止缓存击穿：

```python
arguments={
    'distributed_lock': True,  # 启用分布式锁
}
```

### 5.3 版本号失效策略

保留现有版本号机制，通过递增版本号使整个 namespace 的缓存失效：

```python
cache_manager.increment_version(namespace="role")
# 所有 role 相关的缓存键从 v1 变为 v2，旧键自然过期
```

### 5.4 缓存预热时机

缓存预热应在数据库初始化完成后执行：

```python
# 在 initializers/__init__.py 中调整初始化顺序
# 1. 数据库初始化
# 2. 缓存预热
```

---

## 六、验证方案

### 6.1 功能验证

| 测试项 | 验证方法 | 预期结果 |
|--------|---------|---------|
| L1 缓存读写 | 连续两次读取相同 key | 第二次命中 L1 缓存 |
| L2 缓存读写 | 多进程读取相同 key | 所有进程共享 L2 缓存 |
| 写操作清除缓存 | 修改数据后读取缓存 | 缓存已失效，返回新数据 |
| 版本号失效 | 调用 increment_version | 该 namespace 下缓存全部失效 |
| 缓存击穿防护 | 多线程同时访问过期缓存 | 只触发一次数据库查询 |

### 6.2 性能验证

| 指标 | 目标值 | 测试方法 |
|------|-------|---------|
| L1 读取延迟 | < 1ms | 压力测试 |
| L2 读取延迟 | < 10ms | 压力测试 |
| 缓存命中率 | > 90% | 生产环境监控 |

---

## 七、风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 序列化错误 | 缓存写入失败 | 严格校验缓存值类型 |
| Redis 连接失败 | L2 缓存不可用 | 降级为 L1 缓存，记录日志 |
| 缓存与数据库不一致 | 脏数据 | 使用版本号机制强制失效 |
| 内存占用过高 | OOM | 限制 L1 缓存最大条目数 |

---

## 八、总结

本次重构将：

1. **替换底层实现**：使用成熟的 dogpile.cache 框架替代自定义缓存实现
2. **增强功能**：获得分布式锁、读写分离等高级特性
3. **保持兼容**：对外接口保持一致，业务层改动最小化
4. **提升稳定性**：利用成熟框架的错误处理和稳定性保障

重构后缓存架构：

```
┌─────────────────────────────────────────────────────────────────┐
│                        CacheManager                             │
│  ┌──────────────────┐    读穿透     ┌──────────────────┐       │
│  │ dogpile.cache    │ ────────────→ │ dogpile.cache    │       │
│  │    .memory       │               │    .redis        │       │
│  │  (L1 本地)       │ ←──────────── │  (L2 分布式)     │       │
│  └──────────────────┘    写穿透     └──────────────────┘       │
│          │                         │                           │
│          └─────────────┬───────────┘                           │
│                        ▼                                       │
│              分布式锁（防击穿）                                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   SQLAlchemy Event 监听                         │
│  after_flush → increment_version() → 缓存失效                   │
│  after_commit → cleanup() → 清理脏缓存                          │
└─────────────────────────────────────────────────────────────────┘
```