# 字典管理重构方案

## 一、现状分析

### 当前模型结构
```
src/models/system/
├── dict_type.py   # 字典类型（系统级 + 租户级通过 tenant_id 区分）
└── dict_data.py   # 字典数据（系统级 + 租户级通过 tenant_id 区分）
```

### 当前问题
1. **架构混乱**：平台级和租户级数据混合在同一张表，用 `tenant_id=null` 表示系统级
2. **不符合规范**：与项目其他模块（如平台角色/租户角色）的分层架构不一致
3. **权限隔离不清晰**：平台级和租户级数据权限边界不明确
4. **维护困难**：业务逻辑需要判断 tenant_id 是否为空

## 二、重构目标

参考项目已有的平台级和租户级角色模型设计，将字典管理重构为两套独立系统：

| 层级 | 说明 | 表名 |
|------|------|------|
| **平台级** | 平台管理员管理的通用字典，所有租户共享 | `dict_type` / `dict_data` |
| **租户级** | 租户管理员管理的私有字典，仅该租户可见 | `tenant_dict_type` / `tenant_dict_data` |

## 三、模型设计

### 3.1 平台级字典（保持在 system 目录）

#### DictType（字典类型）
```python
# src/models/system/dict_type.py
# 保持现有表名：dict_type
# 移除 tenant_id 字段
# 其他字段保持不变
```

#### DictData（字典数据）
```python
# src/models/system/dict_data.py
# 保持现有表名：dict_data
# 移除 tenant_id 字段
# 其他字段保持不变
```

### 3.2 租户级字典（新建在 tenant 目录）

#### TenantDictType（租户字典类型）
```python
# src/models/tenant/tenant_dict_type.py
# 表名：tenant_dict_type
# 新增 tenant_id 字段（必填）
# 唯一约束：(tenant_id, code)
# 继承与平台级相同的 Mixin
```

#### TenantDictData（租户字典数据）
```python
# src/models/tenant/tenant_dict_data.py
# 表名：tenant_dict_data
# 新增 tenant_id 字段（必填）
# 关联到 tenant_dict_type 而非 dict_type
```

## 四、具体变更

### 4.1 平台级模型调整

**文件：** `src/models/system/dict_type.py`

**变更：**
- ✅ 保持表名：`dict_type`
- ❌ 移除字段：`tenant_id`
- ✅ 保持其他字段和 Mixin

**文件：** `src/models/system/dict_data.py`

**变更：**
- ✅ 保持表名：`dict_data`
- ❌ 移除字段：`tenant_id`
- ✅ 保持其他字段和 Mixin

### 4.2 租户级模型新建

**新建文件：** `src/models/tenant/tenant_dict_type.py`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BigInteger | 主键 |
| tenant_id | BigInteger | 租户ID（必填，外键） |
| name | String(50) | 字典名称 |
| code | String(50) | 字典编码 |
| ... | ... | 其他继承的字段 |

**唯一约束：** `(tenant_id, code)`

**新建文件：** `src/models/tenant/tenant_dict_data.py`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BigInteger | 主键 |
| tenant_id | BigInteger | 租户ID（必填，外键） |
| dict_type_id | BigInteger | 字典类型ID（外键到 tenant_dict_type） |
| label | String(100) | 字典标签 |
| value | String(100) | 字典值 |
| ... | ... | 其他继承的字段 |

### 4.3 初始化脚本

**任务：**
- 平台级字典：将现有数据中 `tenant_id=null` 的迁移到平台级表
- 租户级字典：将现有数据中 `tenant_id!=null` 的迁移到租户级表
- 注意：数据迁移需要幂等

## 五、API 设计

### 5.1 平台级字典 API
```
GET    /api/v1/system/dict-type       # 平台字典类型列表
POST   /api/v1/system/dict-type       # 新增平台字典类型
PUT    /api/v1/system/dict-type/:id   # 修改平台字典类型
DELETE /api/v1/system/dict-type/:id   # 删除平台字典类型

GET    /api/v1/system/dict-data       # 平台字典数据列表
POST   /api/v1/system/dict-data       # 新增平台字典数据
PUT    /api/v1/system/dict-data/:id   # 修改平台字典数据
DELETE /api/v1/system/dict-data/:id   # 删除平台字典数据
```

### 5.2 租户级字典 API
```
GET    /api/v1/tenant/dict-type       # 租户字典类型列表
POST   /api/v1/tenant/dict-type       # 新增租户字典类型
PUT    /api/v1/tenant/dict-type/:id   # 修改租户字典类型
DELETE /api/v1/tenant/dict-type/:id   # 删除租户字典类型

GET    /api/v1/tenant/dict-data       # 租户字典数据列表
POST   /api/v1/tenant/dict-data       # 新增租户字典数据
PUT    /api/v1/tenant/dict-data/:id   # 修改租户字典数据
DELETE /api/v1/tenant/dict-data/:id   # 删除租户字典数据
```

## 六、权限设计

| 层级 | 可操作角色 |
|------|-----------|
| 平台级字典 | 平台超级管理员、平台管理员 |
| 租户级字典 | 租户所有者、租户管理员 |

## 七、数据迁移方案

### 7.1 迁移步骤
1. 创建新的租户级表 `tenant_dict_type` 和 `tenant_dict_data`
2. 迁移数据：
   - 平台级：`tenant_id IS NULL` → 保留在 `dict_type`/`dict_data`
   - 租户级：`tenant_id IS NOT NULL` → 迁移到 `tenant_dict_type`/`tenant_dict_data`
3. 删除旧表的 `tenant_id` 字段
4. 更新相关业务代码

### 7.2 数据迁移脚本
（后续实施时编写）

## 八、实施计划

### 阶段一：模型层重构
- [ ] 新建租户级字典模型
- [ ] 更新平台级字典模型（移除 tenant_id）
- [ ] 更新 __init__.py 导出
- [ ] 数据库迁移脚本

### 阶段二：业务层重构
- [ ] 平台级字典 Service
- [ ] 租户级字典 Service
- [ ] 权限验证逻辑

### 阶段三：API 层重构
- [ ] 平台级字典路由
- [ ] 租户级字典路由
- [ ] 响应数据结构

### 阶段四：数据迁移
- [ ] 编写迁移脚本
- [ ] 执行数据迁移
- [ ] 验证数据完整性

### 阶段五：前端适配
- [ ] 平台管理端适配
- [ ] 租户管理端适配

## 九、注意事项

1. **向后兼容**：现有 API 需要保持兼容或提供过渡方案
2. **数据安全**：迁移前做好数据备份
3. **系统内置标识**：平台级和租户级都保留 `SystemMixin`，用于区分系统内置和用户自定义
4. **软删除**：保持现有软删除机制
5. **排序**：保持 SortMixin，支持自定义排序

## 十、参考模型

- 平台角色：`src/models/iam/role.py`
- 租户角色：`src/models/tenant/tenant_role.py`
