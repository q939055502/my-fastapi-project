# 缓存键构建修改方案

## 一、现有代码问题分析

基于对 `src/common/core/storage/cache/cache_manager.py` 的分析，当前缓存键构建存在以下问题：

### 1.1 问题列表

| 问题 | 影响 | 严重程度 |
|---|---|---|
| 缺少分层设计 | 所有缓存使用统一格式，无法区分全局静态、单条数据、列表查询 | 高 |
| 使用 `user_id` 而非 `subject_type+subject_id` | 无法支持多种主体类型（用户、成员等） | 高 |
| 缺少 `action` 字段 | 列表查询无法区分不同接口 | 中 |
| 缺少 `query_hash` | 列表查询无法精确区分不同查询条件 | 高 |
| `scope` 位置不符合规范 | 违反分层设计原则 | 中 |

### 1.2 现有缓存键格式

```
cache:{resource}:{tenant_id}:{user_id}[:{scope}]:{key}
```

### 1.3 对比参考方案的差距

根据参考文档，正确的分层格式应该是：

| 缓存类型 | 正确格式 | 当前状态 |
|---|---|---|
| 全局静态 | `global:{resource}:{key}` | ❌ 未实现 |
| 单条数据 | `data:{tenant_id}:{resource}:{subject_type}:{subject_id}:{id}` | ❌ 使用 user_id |
| 列表查询 | `list:{tenant_id}:{resource}:{action}:{scope}:{subject_type}:{subject_id}:{query_hash}` | ❌ 缺少 action、query_hash |

---

## 二、修改方案

### 2.1 新增缓存类型枚举

```python
class CacheType:
    GLOBAL = "global"
    DATA = "data"
    LIST = "list"
```

### 2.2 重构核心方法 `_build_cache_key`

#### 2.2.1 方法签名

```python
def _build_cache_key(
    self,
    cache_type: str,
    resource: str,
    key: Optional[str] = None,
    action: Optional[str] = None,
    scope: Optional[str] = None,
    query_params: Optional[Dict[str, Any]] = None
) -> str:
```

#### 2.2.2 构建逻辑

```python
parts = [cache_type]

# 全局静态缓存：不需要任何权限维度
if cache_type == self.CacheType.GLOBAL:
    parts.append(resource)
    if key:
        parts.append(key)
    return ":".join(parts)

# 所有业务缓存都需要租户ID
tenant_id = get_current_tenant_id()
parts.append(str(tenant_id))

# 所有业务缓存都需要主体类型和主体ID
subject_type = get_current_subject_type()
subject_id = get_current_subject_id()
parts.append(resource)
parts.append(str(subject_type))
parts.append(str(subject_id))

# 单条数据缓存：只需要加数据ID
if cache_type == self.CacheType.DATA:
    if key:
        parts.append(key)
    return ":".join(parts)

# 列表查询缓存：需要加动作、scope和查询参数哈希
if cache_type == self.CacheType.LIST:
    if action:
        parts.append(action)
    if scope:
        parts.append(scope)
    if query_params:
        sorted_params = sorted(query_params.items())
        query_str = "&".join([f"{k}={v}" for k, v in sorted_params])
        query_hash = hashlib.md5(query_str.encode("utf-8")).hexdigest()[:8]
        parts.append(query_hash)

return ":".join(parts)
```

### 2.3 改造现有方法

#### 2.3.1 `get` 方法（直接替换原有方法）

```python
def get(
    self,
    cache_type: str,
    resource: str,
    key: str = None,
    action: str = None,
    include_scope: bool = False,
    query_params: Optional[Dict[str, Any]] = None
) -> Any | None:
    """
    获取缓存（自动注入权限维度）
    
    Args:
        cache_type: 缓存类型（GLOBAL/DATA/LIST）
        resource: 资源名称（如goods、order）
        key: 业务层传入的key（单条数据的ID）
        action: 动作名称（列表查询时使用）
        include_scope: 是否包含数据范围（列表查询必须传True）
        query_params: 查询参数（列表查询时使用）
    
    Returns:
        缓存的值，如果未命中则返回None
    """
    scope = self._get_current_user_scope(resource) if include_scope else None
    full_key = self._build_cache_key(
        cache_type=cache_type,
        resource=resource,
        key=key,
        action=action,
        scope=scope,
        query_params=query_params
    )
    return self._get_raw(full_key)
```

#### 2.3.2 `set` 方法（直接替换原有方法）

```python
def set(
    self,
    cache_type: str,
    resource: str,
    value: Any,
    key: str = None,
    action: str = None,
    include_scope: bool = False,
    query_params: Optional[Dict[str, Any]] = None,
    l1_ttl: int | None = None,
    l2_ttl: int | None = None
) -> None:
    """
    设置缓存（自动注入权限维度）
    
    Args:
        cache_type: 缓存类型（GLOBAL/DATA/LIST）
        resource: 资源名称
        value: 要缓存的值
        key: 业务层传入的key（单条数据的ID）
        action: 动作名称（列表查询时使用）
        include_scope: 是否包含数据范围
        query_params: 查询参数（列表查询时使用）
        l1_ttl: L1缓存过期时间（秒）
        l2_ttl: L2缓存过期时间（秒）
    """
    scope = self._get_current_user_scope(resource) if include_scope else None
    full_key = self._build_cache_key(
        cache_type=cache_type,
        resource=resource,
        key=key,
        action=action,
        scope=scope,
        query_params=query_params
    )
    self._set_raw(full_key, value, l1_ttl, l2_ttl)
```

#### 2.3.3 `delete` 方法（直接替换原有方法）

```python
def delete(
    self,
    cache_type: str,
    resource: str,
    key: str = None,
    action: str = None,
    include_scope: bool = False
) -> None:
    """
    删除指定资源的缓存
    
    Args:
        cache_type: 缓存类型
        resource: 资源名称
        key: 不传则删除该资源下该用户的所有缓存
        action: 动作名称
        include_scope: 是否包含数据范围维度
    """
    scope = self._get_current_user_scope(resource) if include_scope else None
    
    if key:
        full_key = self._build_cache_key(
            cache_type=cache_type,
            resource=resource,
            key=key,
            action=action,
            scope=scope
        )
        self._delete_raw(full_key)
    else:
        pattern = self._build_cache_key(
            cache_type=cache_type,
            resource=resource,
            key="*",
            action=action,
            scope=scope
        )
        self._clear_pattern_raw(pattern)
```

#### 2.3.4 `delete_by_subject` 方法

```python
def delete_by_subject(
    self,
    subject_type: int,
    subject_id: int,
    tenant_id: int = None
) -> None:
    """
    清除指定主体的所有缓存（权限变更时调用）
    
    Args:
        subject_type: 主体类型（0=用户，1=成员）
        subject_id: 主体ID
        tenant_id: 租户ID（不传则使用当前上下文的租户ID）
    """
    tenant_id = tenant_id or get_current_tenant_id() or 0
    pattern = f"*:{tenant_id}:*:{subject_type}:{subject_id}:*"
    self._clear_pattern_raw(pattern)
```

---

## 三、业务层调用方式

### 3.1 全局静态缓存

```python
# 获取全局字典
cache_manager.get(
    cache_type=CacheManager.CacheType.GLOBAL,
    resource="dict",
    key="order_status"
)

# 设置全局缓存
cache_manager.set(
    cache_type=CacheManager.CacheType.GLOBAL,
    resource="dict",
    key="order_status",
    value={"1": "待支付", "2": "已支付"}
)
```

### 3.2 单条数据缓存

```python
# 获取商品详情
cache_manager.get(
    cache_type=CacheManager.CacheType.DATA,
    resource="goods",
    key="123"
)

# 设置商品缓存
cache_manager.set(
    cache_type=CacheManager.CacheType.DATA,
    resource="goods",
    key="123",
    value=goods_data
)
```

### 3.3 列表查询缓存

```python
# 获取商品列表
cache_manager.get(
    cache_type=CacheManager.CacheType.LIST,
    resource="goods",
    action="list",
    include_scope=True,
    query_params={"page": 1, "size": 10, "status": 1}
)

# 设置列表缓存
cache_manager.set(
    cache_type=CacheManager.CacheType.LIST,
    resource="goods",
    value=goods_list,
    action="list",
    include_scope=True,
    query_params={"page": 1, "size": 10, "status": 1}
)
```

---

## 四、缓存失效策略

### 4.1 单条数据缓存失效

```python
# 在 GenericRepository 的 update 和 delete 方法中调用
cache_manager.delete(
    cache_type=CacheManager.CacheType.DATA,
    resource=self.resource_name,
    key=str(id)
)
```

### 4.2 列表查询缓存失效

```python
# 在 GenericRepository 的 create/update/delete 方法中调用
cache_manager.delete(
    cache_type=CacheManager.CacheType.LIST,
    resource=self.resource_name,
    include_scope=True
)
```

### 4.3 权限变更时的全局失效

```python
# 用户权限变更时（用户类型的主体类型为0）
cache_manager.delete_by_subject(subject_type=0, subject_id=user_id)

# 租户删除时
cache_manager.delete(
    cache_type="*",
    resource="*"
)
```

---

## 五、修改后的缓存键格式

| 缓存类型 | 格式 | 示例 |
|---|---|---|
| 全局静态 | `global:{resource}:{key}` | `global:dict:order_status` |
| 单条数据 | `data:{tenant_id}:{resource}:{subject_type}:{subject_id}:{id}` | `data:1001:goods:1:500:123` |
| 列表查询 | `list:{tenant_id}:{resource}:{action}:{scope}:{subject_type}:{subject_id}:{query_hash}` | `list:1001:goods:list:dept:1:500:7a2f9d4c` |

---

## 六、注意事项

### 6.1 缓存键长度控制

当缓存键长度超过 250 字符时，使用 MD5 哈希缩短，但保留资源前缀以便于管理。

### 6.2 权限维度获取

需要确保 `get_current_subject_type()` 和 `get_current_subject_id()` 函数已经在 `src/common/core/context.py` 中实现。

### 6.3 事务一致性

修改后的缓存管理器仍然遵循原有的写穿透策略：更新数据库 → 删除 L1 缓存 → 删除 L2 缓存。

### 6.4 方法命名变更

原有方法全部删除，使用新的统一方法名：

| 删除的方法 | 替代方法 |
|---|---|
| `get(key)` | `get(cache_type, resource, key, ...)` |
| `set(key, value, ...)` | `set(cache_type, resource, value, key, ...)` |
| `delete(key)` | `delete(cache_type, resource, key, ...)` |
| `get_with_permission()` | `get(cache_type, resource, key, ...)` |
| `set_with_permission()` | `set(cache_type, resource, value, key, ...)` |
| `delete_by_resource()` | `delete(cache_type, resource, key, ...)` |
| `delete_by_user()` | `delete_by_subject(subject_type=0, subject_id=user_id)` |

---

## 七、核心文件修改清单

| 文件 | 修改内容 |
|---|---|
| `src/common/core/storage/cache/cache_manager.py` | 新增 CacheType 枚举、重构 `_build_cache_key` 方法、替换 `get/set/delete` 方法、新增 `delete_by_subject` 方法、删除所有旧方法 |
| `src/common/core/context.py` | 新增 `get_current_subject_type()` 和 `get_current_subject_id()` 函数（如未实现） |