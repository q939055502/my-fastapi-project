# 基于现有架构的缓存层权限与租户隔离方案

# 基于你现有架构的缓存层权限与租户隔离方案

完全基于你已有的**事务管理器 \+ GenericRepository \+ 缓存管理器**架构进行适配，不推翻任何现有设计，只做最小化、无侵入的改造，解决缓存层的越权漏洞。

## 一、先对齐你的现有架构与核心问题

### 你的标准数据访问流程（完全正确）

```Plain Text
业务层
   ↓
【事务管理器】→ 获取/释放DB Session
   ↓
【GenericRepository】→ 执行CRUD
   ↓
【SQLAlchemy】→ before_compile自动注入租户+数据权限过滤
   ↓
返回数据库结果 → 业务层调用【缓存管理器】回写缓存
   ↓
返回结果
```

### 唯一的致命漏洞

**缓存的读写完全由业务层手动控制，没有统一的权限维度约束**

- 业务层写缓存时，很容易忘记在缓存键中加入`tenant_id`/`user_id`/`scope`

- 不同租户、不同权限的用户会共享同一个缓存键，导致严重的越权漏洞

- 本地缓存和 Redis 缓存没有统一的管控机制，风险分散

## 二、核心改造原则

1. **不改变任何现有组件的职责**：事务管理器、GenericRepository、缓存管理器的分工保持不变

2. **权限维度自动注入**：所有权限相关的缓存键维度，由缓存管理器**自动添加**，业务层完全不需要关心

3. **写操作自动清缓存**：由 GenericRepository 在数据变更时自动触发缓存清除，不需要业务层手动调用

4. **统一管控所有缓存**：本地缓存和 Redis 缓存全部通过缓存管理器访问，禁止业务层直接使用`lru_cache`或`redis`客户端

## 三、第一步：改造缓存管理器（最核心）

这是唯一需要重点改造的组件，改造后业务层调用缓存的方式完全不变，但缓存键会自动包含所有权限维度。

### 改造后的缓存管理器实现

```python
from functools import lru_cache
import redis
from typing import Any, Optional
from src.common.context import get_current_tenant_id, get_current_user, get_current_user_scope

class CacheManager:
    def __init__(self):
        self.redis_client = redis.Redis()
        # 本地缓存最大容量
        self.local_cache_max_size = 1000

    def _build_cache_key(self, resource: str, key: str, include_scope: bool = False) -> str:
        """
        自动构建包含权限维度的缓存键（核心方法）
        业务层只需要传业务key，权限维度自动注入
        """
        parts = ["cache", resource]
        
        # 1. 强制添加租户ID（最高优先级）
        tenant_id = get_current_tenant_id()
        parts.append(str(tenant_id))
        
        # 2. 强制添加用户ID（个人数据隔离）
        user_id = get_current_user().id if get_current_user() else "anonymous"
        parts.append(str(user_id))
        
        # 3. 可选添加数据范围scope（数据权限隔离）
        if include_scope:
            scope = get_current_user_scope(resource)
            parts.append(scope)
        
        # 4. 最后添加业务层传入的key
        parts.append(key)
        
        return ":".join(parts)

    def get(self, resource: str, key: str, include_scope: bool = False) -> Optional[Any]:
        """
        获取缓存
        :param resource: 资源名称（如goods、order）
        :param key: 业务层传入的key（如id=1、page=1&size=10）
        :param include_scope: 是否包含数据范围（列表查询必须传True）
        """
        full_key = self._build_cache_key(resource, key, include_scope)
        
        # 先查本地缓存（只存全局静态数据，自动过期）
        if self._local_cache_contains(full_key):
            return self._get_local_cache(full_key)
        
        # 再查Redis缓存
        cached_data = self.redis_client.get(full_key)
        if cached_data:
            data = self._deserialize(cached_data)
            # 写入本地缓存（1分钟过期）
            self._set_local_cache(full_key, data, expire=60)
            return data
        
        return None

    def set(self, resource: str, key: str, value: Any, expire: int = 300, include_scope: bool = False) -> None:
        """
        设置缓存
        :param expire: 过期时间（秒），默认5分钟
        """
        full_key = self._build_cache_key(resource, key, include_scope)
        
        # 写入Redis
        self.redis_client.setex(full_key, expire, self._serialize(value))
        
        # 写入本地缓存（最多1分钟，防止本地缓存过期不一致）
        self._set_local_cache(full_key, value, expire=min(expire, 60))

    def delete(self, resource: str, key: str = None, include_scope: bool = False) -> None:
        """
        删除缓存
        :param key: 不传则删除该资源下该用户的所有缓存
        """
        if key:
            full_key = self._build_cache_key(resource, key, include_scope)
            self.redis_client.delete(full_key)
            self._delete_local_cache(full_key)
        else:
            # 删除该资源下该用户的所有缓存
            pattern = self._build_cache_key(resource, "*", include_scope)
            for full_key in self.redis_client.scan_iter(pattern):
                self.redis_client.delete(full_key)
            self._clear_local_cache_by_pattern(pattern)

    def delete_by_user(self, user_id: int, tenant_id: int = None) -> None:
        """
        清除指定用户的所有缓存（权限变更时调用）
        """
        tenant_id = tenant_id or get_current_tenant_id()
        pattern = f"cache:*:{tenant_id}:{user_id}:*"
        for key in self.redis_client.scan_iter(pattern):
            self.redis_client.delete(key)
        self._clear_local_cache_by_pattern(pattern)

    def delete_by_tenant(self, tenant_id: int) -> None:
        """
        清除指定租户的所有缓存（租户删除时调用）
        """
        pattern = f"cache:*:{tenant_id}:*"
        for key in self.redis_client.scan_iter(pattern):
            self.redis_client.delete(key)
        self._clear_local_cache_by_pattern(pattern)

    # 本地缓存实现（内部私有，业务层不可直接访问）
    @lru_cache(maxsize=1000)
    def _get_local_cache(self, key: str) -> Any:
        pass

    def _set_local_cache(self, key: str, value: Any, expire: int) -> None:
        # 本地缓存自动过期实现
        self._get_local_cache.cache_clear()
        self._get_local_cache(key)

    def _local_cache_contains(self, key: str) -> bool:
        return key in self._get_local_cache.cache_info().cache

    def _delete_local_cache(self, key: str) -> None:
        # 简单实现：清除所有本地缓存（生产环境可优化为精确删除）
        self._get_local_cache.cache_clear()

    def _clear_local_cache_by_pattern(self, pattern: str) -> None:
        self._get_local_cache.cache_clear()

    def _serialize(self, data: Any) -> bytes:
        import json
        return json.dumps(data).encode("utf-8")

    def _deserialize(self, data: bytes) -> Any:
        import json
        return json.loads(data.decode("utf-8"))
```

### 业务层调用方式（完全不变）

```python
# 改造前（业务层手动拼接缓存键，容易遗漏权限维度）
cache_key = f"goods:{good_id}"
cached_good = redis.get(cache_key)

# 改造后（业务层只传资源名和业务key，权限维度自动注入）
cached_good = cache_manager.get("goods", f"id={good_id}")

# 列表查询（需要包含数据范围scope）
cached_list = cache_manager.get("goods", f"page={page}&size={size}", include_scope=True)

# 设置缓存
cache_manager.set("goods", f"id={good_id}", good, expire=600)

# 删除单条缓存
cache_manager.delete("goods", f"id={good_id}")

# 删除该用户该资源的所有列表缓存
cache_manager.delete("goods", include_scope=True)
```

## 四、第二步：改造 GenericRepository（自动清缓存）

在你的现有 GenericRepository 基础上，增加写操作后的自动缓存清除逻辑，不需要业务层手动调用。

### 改造后的 GenericRepository

```python
from sqlalchemy.orm import Session
from typing import List, TypeVar, Generic
from src.common.core.cache import cache_manager

T = TypeVar("T")

class GenericRepository(Generic[T]):
    def __init__(self, model: T, resource_name: str):
        self.model = model
        self.resource_name = resource_name  # 资源名称，对应缓存管理器的resource参数

    def get_by_id(self, db: Session, id: int) -> T:
        return db.query(self.model).filter(self.model.id == id).first()

    def list(self, db: Session, **filters) -> List[T]:
        query = db.query(self.model)
        for k, v in filters.items():
            query = query.filter(getattr(self.model, k) == v)
        return query.all()

    def create(self, db: Session, data: dict) -> T:
        instance = self.model(**data)
        db.add(instance)
        db.commit()
        db.refresh(instance)
        
        # 自动清除该资源的所有列表缓存
        cache_manager.delete(self.resource_name, include_scope=True)
        
        return instance

    def update(self, db: Session, id: int, data: dict) -> T:
        instance = self.get_by_id(db, id)
        for k, v in data.items():
            setattr(instance, k, v)
        db.commit()
        db.refresh(instance)
        
        # 自动清除单条缓存和列表缓存
        cache_manager.delete(self.resource_name, f"id={id}")
        cache_manager.delete(self.resource_name, include_scope=True)
        
        return instance

    def delete(self, db: Session, id: int) -> None:
        instance = self.get_by_id(db, id)
        db.delete(instance)
        db.commit()
        
        # 自动清除单条缓存和列表缓存
        cache_manager.delete(self.resource_name, f"id={id}")
        cache_manager.delete(self.resource_name, include_scope=True)

    def batch_delete(self, db: Session, ids: List[int]) -> None:
        db.query(self.model).filter(self.model.id.in_(ids)).delete()
        db.commit()
        
        # 自动清除所有相关缓存
        for id in ids:
            cache_manager.delete(self.resource_name, f"id={id}")
        cache_manager.delete(self.resource_name, include_scope=True)
```

### 业务层使用方式（完全不变）

```python
# 业务层只需要调用Repository的方法，缓存自动清除
goods_repo = GenericRepository(Goods, "goods")
goods_repo.create(db, {"name": "商品1", "price": 100})
# 自动清除goods资源的所有列表缓存，不需要业务层手动调用cache_manager.delete()
```

## 五、第三步：本地缓存的特殊处理

**绝对禁止业务层直接使用****`functools.lru_cache`**，所有本地缓存必须通过缓存管理器访问。

### 错误做法（禁止）

```python
from functools import lru_cache

@lru_cache(maxsize=100)
def get_goods_by_id(good_id: int):
    return goods_repo.get_by_id(db, good_id)
```

### 正确做法（必须）

```python
def get_goods_by_id(good_id: int):
    # 缓存管理器自动处理本地缓存和Redis缓存
    cached = cache_manager.get("goods", f"id={good_id}")
    if cached:
        return cached
    
    good = goods_repo.get_by_id(db, good_id)
    if good:
        cache_manager.set("goods", f"id={good_id}", good, expire=600)
    
    return good
```

## 六、第四步：权限变更时的全局缓存失效

当用户的角色或权限发生变更时，调用缓存管理器的`delete_by_user`方法，立即失效该用户的所有缓存：

```python
def update_user_roles(user_id: int, role_ids: List[int]):
    """更新用户角色"""
    # 更新数据库
    user_role_repo.update_roles(db, user_id, role_ids)
    
    # 立即失效该用户的所有缓存
    cache_manager.delete_by_user(user_id)
```

当租户被删除时，调用`delete_by_tenant`方法，立即失效该租户的所有缓存：

```python
def delete_tenant(tenant_id: int):
    """删除租户"""
    # 删除数据库数据
    tenant_repo.delete(db, tenant_id)
    
    # 立即失效该租户的所有缓存
    cache_manager.delete_by_tenant(tenant_id)
```

## 七、最终完整流程（改造后）

```Plain Text
业务层调用Repository方法
   ↓
【事务管理器】→ 获取DB Session
   ↓
【GenericRepository】→ 执行CRUD
   ↓
【SQLAlchemy】→ before_compile自动注入租户+数据权限过滤
   ↓
【GenericRepository】→ 写操作自动调用缓存管理器清除相关缓存
   ↓
返回数据库结果 → 业务层调用缓存管理器回写缓存
   ↓
【缓存管理器】→ 自动生成包含所有权限维度的缓存键
   ↓
写入Redis和本地缓存
   ↓
返回结果
```

## 八、关键安全红线（必须严格遵守）

1. **所有缓存必须通过缓存管理器访问**，绝对禁止业务层直接使用`redis`客户端或`lru_cache`

2. **缓存键的权限维度必须由缓存管理器自动生成**，业务层绝对不能手动拼接包含`tenant_id`/`user_id`的缓存键

3. **所有写操作必须通过 GenericRepository 执行**，绝对禁止业务层直接操作`db.session`进行增删改

4. **权限变更必须立即调用****`delete_by_user`****方法**，否则旧权限会继续生效

5. **本地缓存只能存储非敏感、低频率变更的数据**，过期时间绝对不能超过 1 分钟

## 九、总结

这个方案完全适配你现有的架构，只需要改造两个核心组件：

1. **缓存管理器**：自动注入权限维度，统一管控所有缓存

2. **GenericRepository**：写操作自动清除缓存

改造后：

- ✅ 业务层代码几乎不需要修改

- ✅ 缓存层的租户隔离和数据权限自动生效

- ✅ 从根本上杜绝了缓存越权的可能

- ✅ 保持了你原有架构的清晰分工

- ✅ 所有权限逻辑集中在两个组件中，便于维护和审计

需要我帮你把这个缓存管理器和 GenericRepository 的改造，整理成可以直接替换你现有代码的完整版本吗？

> （注：文档部分内容可能由 AI 生成）
