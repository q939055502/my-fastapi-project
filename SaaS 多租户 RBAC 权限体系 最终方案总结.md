# SaaS 多租户 RBAC 权限体系 最终方案总结

本方案基于 \\*\\*「操作权限与数据范围解耦、三层分层鉴权、角色为最小约束单元」\\*\\* 的核心设计，适配 SaaS 多租户隔离、动态组织层级、精细化数据管控的需求，兼顾可扩展性、安全性与工程可落地性。

---

## 一、核心模型表设计

共 7 张核心业务表 \+ 1 个通用抽象 Mixin，覆盖权限、角色、组织、数据规则全链路。

### 1\. 权限表 `iam_permission`

- **核心作用**：定义全局唯一的操作权限，仅描述「对某类资源能执行什么动作」，与数据范围完全解耦。表数据永久稳定，不随租户、组织数量增长而膨胀。

- **关键字段说明**：

    - 主键 ID：唯一标识

    - 资源标识 `resource`：如 `user`、`role`、`order`，对应业务资源类型

    - 操作类型 `action`：如 `create`、`read`、`update`、`delete`，对应具体动作

    - 权限类型 `type`：`menu/button/api`，区分前端菜单、按钮、后端接口

    - 适用范围 `applicable_scope`：`platform/tenant`，配置级约束，标记该权限仅平台可用还是租户可绑定

    - 父级 ID `parent_id`：支持树形层级（模块→菜单→按钮）

    - 通用系统字段：名称、排序、状态、创建时间、更新时间、逻辑删除标记

### 2\. 角色表 `iam_role`

- **核心作用**：权限的容器与约束单元，是「操作权限 \+ 数据范围规则」的最小打包载体，分平台级和租户级两类。

- **关键字段说明**：

    - 主键 ID：唯一标识

    - 角色名称、角色编码 `code`：编码用于程序识别，租户内唯一

    - 租户 ID `tenant_id`：`NULL` 代表平台级角色，大于 0 代表归属某租户的私有角色，天然限定角色的默认生效边界

    - 创建人 ID `creator_id`：角色作为业务资源的归属标识，用于数据鉴权比对

    - 部门 ID `dept_id`：可选，角色的组织归属

    - 通用系统字段：排序、状态、备注、创建时间、逻辑删除标记

### 3\. 角色 \- 权限关联表 `iam_role_permission`

- **核心作用**：维护角色与操作权限的多对多绑定关系，实现一个角色绑定多个权限、一个权限被多个角色复用。

- **关键字段说明**：

    - 主键 ID

    - 角色 ID

    - 权限 ID

    - 唯一约束：角色 ID \+ 权限 ID，避免重复绑定

### 4\. 角色 \- 主体关联表 `iam_role_subject`

- **核心作用**：维护用户（主体）与角色的多对多分配关系，实现给用户授予角色。

- **关键字段说明**：

    - 主键 ID

    - 主体 ID `subject_id`：用户 ID / 成员 ID

    - 主体类型 `subject_type`：区分平台用户、租户成员

    - 角色 ID

    - 创建人 ID：记录角色分配人，用于审计

    - 唯一约束：主体 ID \+ 主体类型 \+ 角色 ID

### 5\. 数据范围规则表 `iam_data_scope_rule`

- **核心作用**：精细化数据鉴权的核心表，为「角色 \+ 权限」的组合绑定数据维度约束，替代传统写死在权限表的 scope 枚举，支持灵活扩展。

- **关键字段说明**：

    - 主键 ID

    - 角色 ID

    - 权限 ID

    - 维度类型 `dimension_type`：`tenant`（租户维度）、`org`（组织维度）、`creator`（创建人维度），可扩展自定义维度

    - 匹配方式 `match_type`：`eq`（等于）、`all`（全部）、`tree`（含所有下级）、`in`（多选）

    - 维度值 `dimension_value`：`*` 代表全部，或存具体 ID、多 ID 逗号分隔；仅存规则语义，不与具体数据值耦合

    - 唯一约束：角色 ID \+ 权限 ID \+ 维度类型，同一角色同一权限同一维度仅一条规则

### 6\. 组织节点表 `sys_org`

- **核心作用**：统一承载所有层级的组织架构（分公司 / 部门 / 小组等），树形结构，支持无限层级动态增减，无需为不同层级单独建表。

- **关键字段说明**：

    - 主键 ID

    - 租户 ID

    - 父级 ID `parent_id`：构建树形父子关系

    - 组织名称

    - 通用字段：排序、状态、创建时间

### 7\. 组织闭包表 `sys_org_closure`

- **核心作用**：预存组织树的所有祖先 \- 后代关系，解决树形结构深层查询的性能问题，实现「查询某节点下所有后代」的固定开销，性能与层级深度无关。

- **关键字段说明**：

    - 祖先节点 ID `ancestor_id`

    - 后代节点 ID `descendant_id`

    - 层级距离 `distance`：0 代表自身，1 代表直接子级，以此类推

---

## 二、模型实体关系

### 1\. 核心 RBAC 关系（多对多）

- **用户 ↔ 角色**：通过「角色 \- 主体关联表」实现多对多，一个用户可分配多个角色，一个角色可分配给多个用户。

- **角色 ↔ 操作权限**：通过「角色 \- 权限关联表」实现多对多，一个角色可绑定多个权限，一个权限可被多个角色全局复用。

### 2\. 数据范围关系（一对多）

- **角色 ↔ 数据范围规则**：一对多，一个角色可针对不同权限配置不同的数据维度规则。

### 3\. 组织架构关系

- 组织节点自身为一对多树形关系，通过 `parent_id` 关联。

- 组织节点与组织闭包为一对多关系，一个节点对应多条祖先 / 后代关系。

### 4\. 业务数据关联

所有业务表通过 \\*\\*「租户 ID、创建人 ID」\\*\\* 两个标准字段，与权限体系形成隐式关联，作为数据鉴权的比对依据，无需与权限表直接建立外键。

---

## 三、分层鉴权完整体系

### 3\.1 核心设计原则

**逐角色独立校验，禁止跨角色合并权限与约束**：
角色是「操作权限 \+ 数据范围」的最小打包单元，仅当单个角色同时满足「拥有操作权限 \+ 数据范围覆盖目标资源」时，鉴权才算通过；多角色最终取并集。
该原则从根本上避免「平台权限误用在租户数据上」的越权风险。

### 3\.2 三层鉴权逻辑

按「从粗到细、从功能到数据」的顺序逐层校验，前一层不通过直接终止，兼顾性能与安全。

#### 第一层：操作权限鉴权（功能级门禁）

- **校验目标**：判断用户是否拥有执行该操作的资格，与具体数据无关。

- **校验依据**：遍历用户当前上下文的所有角色，是否至少有一个角色绑定了目标 `resource:action` 权限编码。

- **执行位置**：接口入口层（依赖注入 / 装饰器），是所有请求的第一道门禁。

- **失败返回**：403 无操作权限。

#### 第二层：租户域鉴权（SaaS 硬隔离红线）

- **校验目标**：多租户隔离的强制屏障，判断目标资源是否属于用户可访问的租户范围。

- **校验规则**：

    - 平台级角色：默认仅可操作租户 ID 为 NULL 的平台资源；若配置了 `tenant` 维度 `all` 规则，可操作所有租户数据。

    - 租户级角色：默认仅可操作与角色同租户的资源，绝对禁止跨租户访问。

- **执行位置**：读操作由 ORM 过滤器自动拼接条件，写操作由统一鉴权函数显式校验。

- **失败返回**：403 跨租户越权。

#### 第三层：细化数据鉴权（业务粒度管控）

- **校验目标**：在当前租户内，进一步限定用户可访问的数据粒度，适配个人、小组、部门、分公司等动态组织层级。

- **实现逻辑**：基于「创建人 → 组织树」间接推导范围，业务表无需冗余存储多层组织 ID。

    - `creator` 维度：`self` = 仅本人创建的数据，`all` = 全部创建人

    - `org` 维度：`eq` = 仅本级组织，`tree` = 本级 \+ 所有下级组织

- **执行位置**：读操作自动拼接过滤条件，写操作显式校验。

- **失败返回**：403 无数据访问权限。

### 3\.3 读写场景差异化实现

#### 读操作（列表 / 详情查询）：ORM 层自动过滤

- 实现方式：通过 ORM 事件监听，在 SQL 编译执行前自动追加「租户隔离 \+ 数据范围」过滤条件。

- 业务感知：完全透明，Service 层仅需编写业务筛选条件（如时间、状态），权限条件自动叠加，无需重复编码。

- 核心优势：杜绝遗漏、统一规则、消除大量重复的权限判断代码。

#### 写操作（更新 / 删除）：显式鉴权校验

- 实现流程：先查询目标资源 → 调用统一鉴权函数完成二三层校验 → 校验通过再执行业务操作。

- 核心优势：返回明确的权限错误提示，便于前端交互与审计，避免静默失败导致的业务歧义。

- 新增操作特殊处理：无需数据范围校验，仅校验操作权限，自动填充租户 ID、创建人 ID 等归属字段。

---

## 四、工程化优化与最佳实践

### 4\.1 通用数据范围 Mixin（`DataScopeMixin`）

- **设计目的**：消除业务表的重复字段定义，统一归属字段规范，确保所有需要权限隔离的表结构完全一致。

- **包含能力**：统一提供 `tenant_id`、`creator_id` 两个标准字段，并自动创建索引。

- **使用方式**：所有需要数据隔离的业务表继承该 Mixin 即可，无需重复定义字段，保证全系统字段命名、类型、索引统一。

### 4\.2 ORM 层全局数据过滤器

- 基于 ORM 事件机制实现全局拦截，仅对继承了 `DataScopeMixin` 的模型生效。

- 自动完成两项工作：强制追加租户 ID 过滤、根据当前用户权限 scope 自动追加创建人 / 组织范围过滤。

- 支持例外机制：可通过上下文变量跳过过滤，适配后台任务、数据同步、初始化脚本等特殊场景。

### 4\.3 三级缓存架构

按收益优先级从高到低分层落地，优先做高收益、低风险的上层缓存。

|缓存层级|缓存内容|键设计|收益|优先级|
|---|---|---|---|---|
|一级：IAM 元数据缓存|用户 \+ 当前租户的完整权限上下文（角色、权限编码、数据规则）|`iam:ctx:{user_id}:{current_tenant_id}`|消除每次请求的多表关联查询，命中率接近 100%|必做|
|二级：数据范围推导缓存|组织节点对应的有权限用户 ID 集合（本级、含下级两种）|`iam:scope:user_ids:{org_id}:{scope_type}`|租户内共享，避免重复计算组织子树，性能损耗低|推荐做|
|三级：业务查询结果缓存|热点业务的分页列表、详情数据|`biz:{resource}:list:{权限指纹}:{参数哈希}`|降低高频查询的数据库压力|按需做|

### 4\.4 缓存一致性策略

- **核心原则**：主动失效为主，过期时间兜底。

- **主动失效**：角色分配、权限变更、组织架构调整、用户部门调动时，主动删除对应缓存。

- **过期兜底**：所有缓存必须设置过期时间，防止漏删导致永久脏数据。

- **写操作规范**：更新数据库后删除缓存（而非更新缓存），规避并发竞态导致的数据不一致问题。

---

## 五、边界说明与扩展建议

### 5\.1 超级管理员豁免机制

- 平台超级管理员：通过白名单配置跳过所有三层鉴权，拥有全平台全部权限，逻辑不写死在业务代码中。

- 租户超级管理员：在当前租户内跳过第三层数据细化校验，默认拥有租户内全部数据权限。

### 5\.2 权限审计与安全

- 记录所有权限变更操作：角色创建、权限分配、角色授权、管理员操作，支持全链路追溯。

- 记录越权访问尝试与敏感数据操作，满足等保合规与安全审计要求。

### 5\.3 批量操作处理

采用「先过滤、后操作」的安全方案：批量操作前先按数据范围过滤出有权限的 ID 集合，仅对合法 ID 执行操作，兼顾权限安全与执行性能。禁止全量操作后再过滤结果。

### 5\.4 后续扩展方向

1. **字段级权限**：针对敏感字段实现可见、可编辑、脱敏控制，在数据范围基础上进一步细化管控粒度。

2. **临时权限**：支持限时授予角色 / 权限，到期自动回收，适配临时协作、代岗等场景。

3. **第三方身份联动**：对接企业微信、钉钉等身份源，自动同步组织架构、映射角色与数据范围。

4. **权限模板**：预置行业通用角色模板，新租户一键导入，降低交付与配置成本。

> （注：文档部分内容可能由 AI 生成）
