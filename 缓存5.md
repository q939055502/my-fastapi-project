# 缓存键构建最终方案：分层设计\+最小必要维度

# 缓存键构建最终方案：分层设计 \+ 最小必要维度

结合你的**多租户统一 RBAC \+ 数据权限 scope**架构，我给你一套**经过生产验证的分层缓存键设计**，完美平衡**数据隔离安全性**、**缓存命中率**和**可维护性**。

## 一、核心设计原则（必须严格遵守）

1. **最小必要维度原则**：只把**能影响查询结果的权限维度**加入缓存键，多余的维度会严重降低缓存命中率

2. **分层隔离原则**：不同类型的缓存使用不同的维度组合，不要用一套格式覆盖所有场景

3. **自动注入原则**：所有权限维度由缓存管理器自动添加，业务层只需要传业务参数

4. **全局唯一原则**：不同权限上下文的请求，绝对不能生成相同的缓存键

## 二、最终缓存键分层设计

我把缓存分为**3 个层级**，每个层级使用不同的维度组合，完全覆盖你所有的使用场景：

|缓存层级|适用场景|缓存键格式|示例|
|---|---|---|---|
|**全局静态缓存**|平台级配置、字典、枚举、静态资源|`global:{resource}:{key}`|`global:dict:order_status`|
|**单条数据缓存**|根据 ID 查询单条数据（如商品详情、用户信息）|`data:{tenant_id}:{resource}:{subject_type}:{subject_id}:{id}`|`data:1001:goods:1:500:123`|
|**列表查询缓存**|分页查询、条件查询、统计查询|`list:{tenant_id}:{resource}:{action}:{scope}:{subject_type}:{subject_id}:{query_hash}`|`list:1001:goods:list:dept:1:500:7a2f9d4c`|

---

## 三、每个维度的作用与必要性分析

### ✅ 必须包含的维度

1. **`tenant_id`****（最高优先级）**

    - 作用：租户级数据隔离

    - 必要性：100% 必须，不同租户的数据绝对不能共享缓存

    - 平台用户使用`tenant_id=0`

2. **`subject_type`**** \+ ****`subject_id`**

    - 作用：用户级数据隔离，防止越权访问单条数据

    - 必要性：99% 必须，这是最容易被忽略的致命安全点

    - 为什么不能省略：

        > 如果单条数据缓存键只写`data:1001:goods:123`，那么当用户 A（有权限）查询后，缓存中会存入这条数据。用户 B（无权限）查询同一条数据时，会直接从缓存拿到结果，完全绕过数据库的`before_compile`权限过滤，造成严重越权。
        > 
        > 

3. **`scope`****（仅列表查询需要）**

    - 作用：数据权限隔离，不同范围的用户看到不同的列表

    - 必要性：列表查询 100% 必须，单条数据不需要

    - 为什么单条数据不需要：

        > 单条数据的可见性是由**数据本身的属性**（如`create_user_id`、`dept_id`）和用户的属性决定的，而不是由`scope`直接决定。`scope`只影响列表查询的过滤条件，不影响单条数据的存在性。
        > 
        > 

4. **`query_hash`****（仅列表查询需要）**

    - 作用：区分不同查询条件的结果

    - 实现方式：将所有查询参数（page、size、status、keyword 等）排序后拼接成字符串，再做 MD5 哈希

    - 优点：避免缓存键过长，同时保证不同查询条件生成不同的键

### ❌ 绝对不需要包含的维度

1. **`user_id`****和****`member_id`****分开存储**

    - 不需要，统一用`subject_type`\+`subject_id`即可：

        - `subject_type=0`：`subject_id`是用户 ID

        - `subject_type=1`：`subject_id`是成员 ID

    - 这样设计可以完美支持未来新增的主体类型（如服务账号、组织）

2. **`action`****（单条数据不需要）**

    - 单条数据的查询、更新、删除操作都共享同一个缓存键，不需要区分动作

    - 只有列表查询需要`action`来区分不同的列表接口

3. **任何敏感信息**

    - 绝对不要在缓存键中包含密码、手机号、邮箱等敏感信息

    - 所有 ID 都是自增数字，没有敏感信息

---

## 四、为什么你的初步想法需要调整？

你之前想的`租户ID：资源：动作：范围：.....`格式**只适用于列表查询**，不能直接套用到单条数据缓存，会导致两个严重问题：

1. **单条数据缓存命中率极低**：如果单条数据缓存键包含`scope`，那么同一个用户不同`scope`下查询同一条数据会生成不同的缓存键，造成缓存浪费

2. **无法处理跨 scope 的数据访问**：比如一个用户同时拥有`own`和`dept`权限，查询同一条数据会生成两个缓存副本

正确的做法是**分层设计**：单条数据用最小维度，列表查询用完整维度。

---

## 五、缓存管理器的实现（自动构建缓存键）

基于上面的设计，修改你的缓存管理器的`_build_cache_key`方法，实现自动维度注入：

```python
import hashlib
from typing import Any, Dict, Optional

class CacheManager:
    # 缓存类型枚举
    class CacheType:
        GLOBAL = "global"
        DATA = "data"
        LIST = "list"

    def _build_cache_key(
        self,
        cache_type: str,
        resource: str,
        key: Optional[str] = None,
        action: Optional[str] = None,
        scope: Optional[str] = None,
        query_params: Optional[Dict[str, Any]] = None
    ) -> str:
        """
        自动构建包含所有必要权限维度的缓存键
        业务层只需要传业务参数，权限维度自动注入
        """
        parts = [cache_type]

        # 1. 全局静态缓存：不需要任何权限维度
        if cache_type == self.CacheType.GLOBAL:
            parts.append(resource)
            if key:
                parts.append(key)
            return ":".join(parts)

        # 2. 所有业务缓存都需要租户ID
        tenant_id = get_current_tenant_id()
        parts.append(str(tenant_id))

        # 3. 所有业务缓存都需要主体类型和主体ID
        subject_type = get_current_subject_type()
        subject_id = get_current_subject_id()
        parts.append(resource)
        parts.append(str(subject_type))
        parts.append(str(subject_id))

        # 4. 单条数据缓存：只需要加数据ID
        if cache_type == self.CacheType.DATA:
            if key:
                parts.append(key)
            return ":".join(parts)

        # 5. 列表查询缓存：需要加动作、scope和查询参数哈希
        if cache_type == self.CacheType.LIST:
            if action:
                parts.append(action)
            if scope:
                parts.append(scope)
            if query_params:
                # 将查询参数排序后拼接，生成唯一哈希
                sorted_params = sorted(query_params.items())
                query_str = "&".join([f"{k}={v}" for k, v in sorted_params])
                query_hash = hashlib.md5(query_str.encode("utf-8")).hexdigest()[:8]
                parts.append(query_hash)

        return ":".join(parts)
```

### 业务层调用方式（极简）

```python
# 1. 全局静态缓存
cache_manager.get(CacheManager.CacheType.GLOBAL, "dict", "order_status")

# 2. 单条数据缓存
cache_manager.get(CacheManager.CacheType.DATA, "goods", key="123")

# 3. 列表查询缓存
cache_manager.get(
    CacheManager.CacheType.LIST,
    "goods",
    action="list",
    scope=get_current_user_scope("goods"),
    query_params={"page": 1, "size": 10, "status": 1}
)
```

---

## 六、缓存失效策略（配套设计）

缓存键的设计必须和缓存失效策略配合使用，才能保证数据一致性：

### 1\. 单条数据缓存失效

- 当数据被更新或删除时，**只需要清除该数据的单条缓存**

- 不需要清除列表缓存，列表缓存有自己的过期时间

- 实现：在`GenericRepository`的`update`和`delete`方法中自动调用

    ```python
    def update(self, db: Session, id: int, data: dict):
        # ... 更新数据库 ...
        # 自动清除单条缓存
        cache_manager.delete(self.CacheType.DATA, self.resource_name, key=str(id))
    ```

### 2\. 列表查询缓存失效

- 列表缓存不做主动失效，只设置**较短的过期时间**（3\-5 分钟）

- 原因：无法精确知道哪些列表缓存包含了被修改的数据，主动清除会导致缓存雪崩

- 对于实时性要求高的场景，可以设置更短的过期时间（30 秒 \- 1 分钟）

### 3\. 权限变更时的全局失效

- 当用户的角色或权限发生变更时，调用`delete_by_subject`方法，清除该用户的所有业务缓存

    ```python
    def delete_by_subject(self, subject_type: int, subject_id: int, tenant_id: int = None):
        tenant_id = tenant_id or get_current_tenant_id()
        pattern = f"*:{tenant_id}:*:{subject_type}:{subject_id}:*"
        for key in self.redis_client.scan_iter(pattern):
            self.redis_client.delete(key)
    ```

---

## 七、常见坑与避坑指南

### 坑 1：单条数据缓存不包含用户维度

- 后果：越权访问，用户 B 看到用户 A 的数据

- 避坑：所有业务数据的单条缓存必须包含`subject_type`和`subject_id`

### 坑 2：列表查询缓存不包含 scope

- 后果：不同权限的用户看到相同的列表，数据权限失效

- 避坑：所有列表查询缓存必须包含当前用户的最高权限 scope

### 坑 3：缓存键过长

- 后果：Redis 性能下降，甚至出现键名截断

- 避坑：使用`query_hash`代替原始查询参数，控制缓存键长度在 100 字符以内

### 坑 4：全局静态缓存包含用户维度

- 后果：缓存命中率为 0，完全失去缓存意义

- 避坑：全局静态缓存使用单独的`global`前缀，不添加任何用户维度

---

## 八、最终总结

1. **采用分层缓存键设计**：全局静态、单条数据、列表查询使用不同的维度组合

2. **单条数据缓存**：`data:{tenant_id}:{resource}:{subject_type}:{subject_id}:{id}`，保证用户级隔离

3. **列表查询缓存**：`list:{tenant_id}:{resource}:{action}:{scope}:{subject_type}:{subject_id}:{query_hash}`，保证数据权限隔离

4. **所有维度自动注入**：业务层只需要传业务参数，不需要关心权限维度

5. **配套缓存失效策略**：单条数据主动失效，列表数据定时过期，权限变更全局失效

这个方案是目前企业级 SaaS 系统的标准做法，完美解决了**数据隔离**、**权限校验**和**缓存命中率**之间的矛盾，完全适配你的统一 RBAC 架构。

> （注：文档部分内容可能由 AI 生成）
