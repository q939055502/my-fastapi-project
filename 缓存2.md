## 业界通用做法总结

根据我的经验，业界主流的缓存设计通常遵循以下模式：

---

### 一、缓存分类（两层模型）

几乎所有项目都会分成两类：

| 类型 | 通用命名 | 键结构 | 失效策略 |
|------|---------|--------|---------|
| **原表缓存** | Entity Cache / Data Cache | `{resource}:{id}` | 数据变更时精确删除 |
| **业务缓存** | Query Cache / View Cache | `{resource}:{query_hash}` | TTL 为主，事件驱动为辅 |

---

### 二、缓存键设计（标准化）

#### 1. 原表缓存（最简单）

```
# 通用格式
{prefix}:{resource}:{id}

# 示例
cache:user:123
cache:product:456
cache:order:789
```

**特点**：
- 键只包含 `资源名 + 主键`，**不混入任何业务参数**
- 所有请求共享同一缓存，命中率最高
- 权限过滤在读取后进行（业务层负责）

#### 2. 业务缓存（带参数）

```
# 通用格式
{prefix}:{resource}:{query_hash}

# 示例（query_hash 是参数的 md5）
cache:product_list:abc123def456
cache:order_search:789ghi012jkl
```

**特点**：
- `query_hash` 通常是查询参数排序后拼接的哈希值
- 只关心"查询条件是什么"，不关心"谁在查"
- 相同查询条件共享缓存

---

### 三、失效策略（组合使用）

业界通常**不依赖单一策略**，而是组合使用：

#### 1. TTL 兜底（必备）

```python
# 所有缓存都设置 TTL
redis.setex("cache:user:123", 3600, value)  # 1小时过期
redis.setex("cache:product_list:abc123", 300, value)  # 5分钟过期
```

**原则**：
- 数据越稳定，TTL 越长
- 列表缓存 TTL 通常比单条缓存短

#### 2. 事件驱动失效（核心）

```python
# 更新用户后，精确删除
def update_user(user_id, data):
    db.update(data)
    redis.delete(f"cache:user:{user_id}")
    # 可选：删除相关列表缓存
    redis.delete(f"cache:user_list:{query_hash}")
```

**关键**：只删除**明确相关**的缓存，不做模糊删除（如 `delete_by_pattern`）

#### 3. 版本号机制（特定场景）

```python
# 全局配置变更时使用
def update_config():
    db.update(config)
    new_version = redis.incr("cache:version:config")
    # 后续请求使用新 version，旧缓存自然失效
```

**适用场景**：
- 全局配置
- 字典数据
- 需要批量失效但难以追踪依赖的场景

---

### 四、租户隔离（权限处理）

这是你最关心的问题，业界主要有两种做法：

#### 做法一：缓存层不做隔离（推荐）

```
┌─────────────────────────────────────────────────────┐
│                 推荐方案                              │
├─────────────────────────────────────────────────────┤
│                                                     │
│  缓存层：cache:product:456  ← 所有租户共享          │
│         ↑                                           │
│  业务层：检查租户权限 ← 读取后过滤                    │
│                                                     │
│  优点：                                              │
│  - 缓存命中率最高                                    │
│  - 实现简单                                          │
│  - 键结构清晰                                        │
│                                                     │
│  缺点：                                              │
│  - 需要在业务层做权限校验                            │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**代表项目**：几乎所有大型电商系统（淘宝、京东等）

#### 做法二：缓存层做隔离（特定场景）

```
# 键中加入 tenant_id
cache:tenant:1:product:456
cache:tenant:2:product:456
```

**适用场景**：
- 同一资源在不同租户间数据完全不同（如多租户 SaaS 系统）
- 租户间数据需要物理隔离（合规要求）

**注意**：即使做隔离，也只用 `tenant_id`，不会用 `subject_type/subject_id`

---

### 五、列表缓存（查询结果）

#### 通用做法

```python
# 写入时
def list_products(query_params):
    query_hash = md5(sorted(query_params.items()))
    cache_key = f"cache:product_list:{query_hash}"
    
    result = redis.get(cache_key)
    if result:
        return result
    
    result = db.query(query_params)
    redis.setex(cache_key, 300, result)
    return result

# 失效时（产品数据变更）
def update_product(product_id):
    db.update(product_id)
    redis.delete(f"cache:product:{product_id}")
    # 方案A：不主动删除列表缓存，依赖TTL过期
    # 方案B：使用Tag机制删除相关列表缓存
```

#### 列表缓存失效的三种策略

| 策略 | 实现方式 | 适用场景 |
|------|---------|---------|
| **仅 TTL** | 不主动删除，等待过期 | 数据变更不频繁 |
| **Tag 机制** | 维护列表与数据的关联 | 需要实时一致性 |
| **消息队列** | 变更时发送消息，消费者清理 | 高并发场景 |

---

### 六、业界最佳实践总结

```
┌─────────────────────────────────────────────────────────┐
│                   业界通用原则                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. 缓存键越简单越好，只包含必要维度                      │
│  2. 原表缓存和业务缓存分开处理                            │
│  3. TTL 是兜底，事件驱动是核心                            │
│  4. 权限隔离放在业务层，不污染缓存键                       │
│  5. 列表缓存优先依赖 TTL，必要时用 Tag                    │
│  6. 避免模糊删除（如 KEYS *），影响性能                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

### 七、对比当前项目

当前项目的设计与业界通用做法的差异：

| 维度 | 业界通用 | 当前项目 |
|------|---------|---------|
| 缓存键复杂度 | 简单（资源+ID/查询哈希） | 复杂（多维度拼接） |
| 权限隔离 | 业务层处理 | 缓存键中处理 |
| 列表失效 | TTL + Tag | 版本号 + 模糊删除 |
| subject_type/subject_id | 不存在（业务概念） | 存在（下沉到 core） |

**结论**：当前项目的设计**过度设计**了，引入了不必要的复杂度。业界普遍采用更简单、更清晰的方案。

如果你想让我帮你重构到业界通用模式，告诉我！