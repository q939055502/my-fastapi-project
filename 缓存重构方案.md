# 缓存底层重构方案

> 范围：仅缓存底层（CacheManager / CacheKey / TagStore / ORM Events）
> 不含：缓存预热、业务层迁移、租户隔离重构（后续单独进行）

---

## 一、目标与范围

### 1.1 目标

将当前「单 namespace 版本号」机制升级为「多标签版本哈希」机制，实现：

1. **精准失效**：从表级整体失效升级为行级精准失效，提升缓存命中率
2. **多维护联**：一份缓存可打多个标签，任一标签变更即失效，支撑 RBAC 等多维依赖场景
3. **统一失效策略**：消除当前两套 ORM 事件 + 物理删除 + 版本号并存的混乱
4. **全新接口**：以 tags 为一等公民重新设计接口，旧接口标记废弃

### 1.2 本次范围

| 模块 | 是否重构 | 说明 |
|------|---------|------|
| `cache_manager.py` | 是 | 核心重写：tag 机制 + 新接口 |
| `cache_key.py` | 是 | 键格式适配 tag_hash |
| `tag_store.py` | 是 | 新增：标签版本存储管理 |
| `orm_events.py` | 是 | 重写：模型→标签映射 |
| `dogpile_config.py` | 微调 | 适配 tag 版本读取方式 |
| `__init__.py` | 是 | 导出更新 |
| `rbac/cache_events.py` | 删除 | 消除重复监听 |
| 缓存预热 (`cache_warmup_initializer.py`) | 否 | 后续单独重构 |
| 业务层调用方 (permission_control, login_ctx 等) | 否 | 后续逐步迁移，旧接口暂保留废弃包装 |

### 1.3 设计决策

| 决策 | 选择 | 原因 |
|------|------|------|
| 哈希算法 | Python 内置 `hashlib`（md5 截取 16 位 hex） | 零新依赖，性能足够 |
| 接口策略 | 全新接口设计，旧接口标记废弃 | tag 作为一等公民，接口更清晰 |
| CacheType | 保留 GLOBAL/DATA/LIST 三层 | tag 管失效，CacheType 管键结构，正交不冲突 |

---

## 二、现状分析

### 2.1 当前架构

```
CacheManager
├── 版本号管理: _get_version / _set_version / increment_version
│   └── Redis key: cache:version:{namespace}  (单个 namespace = 一个 resource)
├── 缓存读写: get / set / delete (嵌入 version 到 key)
├── 便捷方法: get_global / set_global / delete_global
└── 装饰器: cached

cache_key.py
└── 键格式: {cache_type}:v{version}:{tenant_id}:{resource}:{key}

orm_events.py (core/storage/cache/)
└── after_flush → increment_version(namespace=resource)

cache_events.py (foundation/iam/rbac/)  ← 重复！
└── after_flush → _clear_pattern_raw(物理删除)
```

### 2.2 核心问题

| 问题 | 影响 |
|------|------|
| 两套 ORM 事件重复注册 `after_flush` | 同一 flush 触发两套失效逻辑，可能不一致 |
| 版本号粒度太粗（表级） | 改一个角色 → 所有角色缓存全失效，命中率低 |
| `permission_control` 的 resource 名 (`rbac:role`) 与 ORM 映射 (`role`) 不一致 | RBAC 缓存无法被 ORM 事件自动清除 |
| 物理删除 (`_clear_pattern_raw`) 与版本号机制混用 | 策略不统一，维护困难 |
| 旧接口无 tags 概念 | 无法支撑多维度联动失效 |

---

## 三、新架构设计

### 3.1 架构总览

```
┌─────────────────────────────────────────────────┐
│                  CacheManager                    │
│                                                  │
│  ┌──────────────┐  ┌──────────────────────────┐ │
│  │  TagStore    │  │  dogpile.cache (L1+L2)   │ │
│  │  (控制平面)   │  │  (数据平面)               │ │
│  │              │  │                          │ │
│  │ 标签版本管理  │  │ 业务缓存数据读写          │ │
│  │ Redis INCR   │  │ L1: memory  L2: redis    │ │
│  │ 本地短时缓存  │  │ 防击穿/锁机制             │ │
│  └──────────────┘  └──────────────────────────┘ │
│                                                  │
│  对外接口: get / set / invalidate / cached       │
└─────────────────────────────────────────────────┘
```

**控制平面与数据平面分离**：
- **TagStore**（控制平面）：管理标签版本元数据，决定缓存键格式
- **dogpile.cache**（数据平面）：管理业务缓存数据读写，完全保留现有 L1+L2 机制

### 3.2 标签版本机制

#### 标签的物理存储

每个标签对应一个独立的 Redis String 键，使用 `INCR` 原子递增：

```
Redis key:  cache_tag:version:{tag_name}
Value:      整数，从 0 开始递增

示例:
  cache_tag:version:role:all      → 3
  cache_tag:version:role:1        → 5
  cache_tag:version:tenant:1001   → 2
  cache_tag:version:perm:all      → 7
```

#### 本地短时缓存

为减少 Redis 查询，TagStore 在本地内存缓存标签版本，TTL = 2 秒：

```python
# 本地缓存结构
_local_tag_cache: dict[str, tuple[int, float]]  # tag -> (version, expire_at)
```

- 读取时先查本地缓存，未过期直接返回
- `invalidate` 后立即清除本地缓存，强制下次读取拉取最新值
- 最多 2 秒不一致窗口，对绝大多数业务可接受

#### 标签哈希计算

```python
def _calc_tag_hash(self, tags: list[str]) -> str:
    """根据标签列表计算当前版本哈希"""
    if not tags:
        return "v0"  # 无标签 = 静态哈希，仅靠 TTL 失效

    sorted_tags = sorted(tags)  # 字典序排序，保证顺序一致
    version_parts = [f"{t}={self._get_tag_version(t)}" for t in sorted_tags]
    version_str = "|".join(version_parts)
    return hashlib.md5(version_str.encode("utf-8")).hexdigest()[:16]
```

示例：
```
tags = ["role:1", "tenant:1001"]
→ version_str = "role:1=5|tenant:1001=2"
→ tag_hash = "a7f2d9e3b1c4f8a2"
```

### 3.3 新缓存键格式

旧格式（version 嵌入）：
```
GLOBAL: global:v{version}:{resource}:{key}
DATA:   data:v{version}:{tenant_id}:{resource}:{key}
LIST:   list:v{version}:{tenant_id}:{resource}:{query_hash}
```

新格式（tag_hash 嵌入）：
```
GLOBAL: global:{tag_hash}:{resource}:{key}
DATA:   data:{tag_hash}:{tenant_id}:{resource}:{key}
LIST:   list:{tag_hash}:{tenant_id}:{resource}:{query_hash}
```

差异仅在于 `v{version}` → `{tag_hash}`，结构不变，CacheType 保留。

### 3.4 标签粒度模型

采用三层标签模型（粗细搭配）：

| 层级 | 格式 | 失效范围 | 适用场景 |
|------|------|---------|---------|
| 表级（粗） | `{entity}:all` | 整张表的缓存全失效 | 兜底、批量操作 |
| 行级（细） | `{entity}:{id}` | 仅失效关联该行的缓存 | 单条记录修改 |
| 租户级 | `tenant:{id}` | 租户下所有缓存失效 | 租户注销、租户隔离 |

示例：
```python
# 缓存用户1的权限数据，打三个标签
cache_manager.set(
    cache_type=CacheType.DATA,
    resource="user_perm",
    key="1",
    value=perm_codes,
    tags=["role:1", "role:2", "tenant:1001"],  # 依赖角色1、角色2、租户1001
    tenant_id=1001,
)

# 修改角色1 → invalidate(["role:1", "role:all"])
# → tag_hash 变化 → 旧缓存键无人访问 → TTL 自然淘汰
```

---

## 四、核心实现细节

### 4.1 文件结构

```
src/core/storage/cache/
├── __init__.py              # 导出更新
├── cache_manager.py         # 核心重写
├── cache_key.py             # 修改：version → tag_hash
├── tag_store.py             # 新增：标签版本存储
├── dogpile_config.py        # 微调：移除旧版本读取，保留 L1/L2 读写
└── orm_events.py            # 重写：模型→标签映射

src/foundation/iam/rbac/
└── cache_events.py          # 删除
```

### 4.2 TagStore（新增 `tag_store.py`）

```python
"""标签版本存储（控制平面）

管理标签的版本元数据，独立于业务缓存数据。
- 权威源: Redis (cache_tag:version:{tag})
- 本地缓存: 2 秒 TTL，减少 Redis 查询
- 失效方式: INCR 原子递增，旧键自然过期
"""

import hashlib
import time
from typing import Any

from . import dogpile_config as _dc

TAG_VERSION_PREFIX = "cache_tag:version:"
_LOCAL_CACHE_TTL = 2  # 秒


class TagStore:
    """标签版本存储"""

    def __init__(self):
        self._local_cache: dict[str, tuple[int, float]] = {}

    def get_tag_version(self, tag: str) -> int:
        """获取标签版本号（先查本地缓存，再查 Redis）"""
        now = time.time()

        # 查本地缓存
        if tag in self._local_cache:
            version, expire_at = self._local_cache[tag]
            if now < expire_at:
                return version

        # 查 Redis 权威值
        if _dc.is_l2_available():
            key = f"{TAG_VERSION_PREFIX}{tag}"
            version = _dc.l2_get_int(key)
        else:
            version = 0

        self._local_cache[tag] = (version, now + _LOCAL_CACHE_TTL)
        return version

    def increment_tag(self, tag: str) -> int:
        """原子递增标签版本，返回递增后的值"""
        # 清除本地缓存，下次读取强制拉最新值
        self._local_cache.pop(tag, None)

        if _dc.is_l2_available():
            key = f"{TAG_VERSION_PREFIX}{tag}"
            return _dc.l2_incr(key)
        return 0

    def calc_tag_hash(self, tags: list[str] | None) -> str:
        """根据标签列表计算版本哈希"""
        if not tags:
            return "v0"

        sorted_tags = sorted(tags)
        version_parts = [f"{t}={self.get_tag_version(t)}" for t in sorted_tags]
        version_str = "|".join(version_parts)
        return hashlib.md5(version_str.encode("utf-8")).hexdigest()[:16]

    def invalidate(self, tags: list[str]) -> None:
        """递增一组标签的版本号"""
        for tag in tags:
            self.increment_tag(tag)


tag_store = TagStore()
```

### 4.3 CacheManager（重写 `cache_manager.py`）

```python
"""统一缓存管理器（Tag 版本哈希型）

封装 L1(本地内存) + L2(Redis) 二级缓存。
通过 TagStore 管理标签版本，标签版本变化 → tag_hash 变化 → 旧缓存键自然失效。

缓存键格式:
    - GLOBAL: global:{tag_hash}:{resource}:{key}
    - DATA:   data:{tag_hash}:{tenant_id}:{resource}:{key}
    - LIST:   list:{tag_hash}:{tenant_id}:{resource}:{query_hash}

核心概念:
    - tags: 缓存依赖的标签列表，任一标签版本变化则缓存失效
    - tag_hash: 由所有标签+版本号计算出的哈希，嵌入缓存键
    - invalidate(tags): 递增标签版本，使关联缓存逻辑失效
"""

import functools
from collections.abc import Callable
from typing import Any

from src.core.config import settings

from .cache_key import CacheType, build_cache_key
from . import dogpile_config as _dc
from .tag_store import tag_store


class CacheManager:
    """统一缓存管理器"""

    # ====================
    # 新接口（推荐使用）
    # ====================

    def get(
        self,
        cache_type: str,
        resource: str,
        key: str | None = None,
        tags: list[str] | None = None,
        query_params: dict[str, Any] | None = None,
        tenant_id: int = 0,
    ) -> Any | None:
        """读取缓存

        Args:
            cache_type: 缓存类型 (CacheType.GLOBAL / DATA / LIST)
            resource: 资源名称
            key: 数据主键 (GLOBAL/DATA 类型使用)
            tags: 依赖标签列表，用于精准失效
            query_params: 查询参数 (LIST 类型使用)
            tenant_id: 租户ID (DATA/LIST 类型使用)
        """
        tag_hash = tag_store.calc_tag_hash(tags)
        full_key = build_cache_key(
            cache_type=cache_type,
            resource=resource,
            key=key,
            query_params=query_params,
            tenant_id=tenant_id,
            tag_hash=tag_hash,
        )
        return self._get_raw(full_key)

    def set(
        self,
        cache_type: str,
        resource: str,
        value: Any,
        key: str | None = None,
        tags: list[str] | None = None,
        query_params: dict[str, Any] | None = None,
        tenant_id: int = 0,
        l1_ttl: int | None = None,
        l2_ttl: int | None = None,
    ) -> None:
        """写入缓存"""
        tag_hash = tag_store.calc_tag_hash(tags)
        full_key = build_cache_key(
            cache_type=cache_type,
            resource=resource,
            key=key,
            query_params=query_params,
            tenant_id=tenant_id,
            tag_hash=tag_hash,
        )
        self._set_raw(full_key, value, l1_ttl, l2_ttl)

    def invalidate(self, tags: list[str]) -> None:
        """失效缓存（递增标签版本，旧缓存键自然过期）

        Args:
            tags: 要失效的标签列表，如 ["role:1", "role:all"]
        """
        tag_store.invalidate(tags)

    def cached(
        self,
        resource: str,
        tags: list[str] | None = None,
        l1_ttl: int | None = None,
        l2_ttl: int | None = None,
        cache_type: str = None,
        tenant_id: int = 0,
    ) -> Callable:
        """装饰器：自动缓存方法返回值"""
        def decorator(func: Callable) -> Callable:
            @functools.wraps(func)
            def wrapper(*args, **kwargs) -> Any:
                key_parts = [func.__name__]
                for i, arg in enumerate(args[1:], 1):
                    if arg is not None:
                        key_parts.append(f"arg{i}_{arg}")
                for k, v in sorted(kwargs.items()):
                    if v is not None:
                        key_parts.append(f"{k}_{v}")
                key = "_".join(key_parts)

                final_cache_type = cache_type or CacheType.DATA
                cached_result = self.get(
                    cache_type=final_cache_type,
                    resource=resource,
                    key=key,
                    tags=tags,
                    tenant_id=tenant_id,
                )
                if cached_result is not None:
                    return cached_result

                result = func(*args, **kwargs)
                if result is not None:
                    self.set(
                        cache_type=final_cache_type,
                        resource=resource,
                        key=key,
                        value=result,
                        tags=tags,
                        l1_ttl=l1_ttl,
                        l2_ttl=l2_ttl,
                        tenant_id=tenant_id,
                    )
                return result
            return wrapper
        return decorator

    # ====================
    # 底层读写（不变）
    # ====================

    def _calculate_l1_ttl(self, base_ttl: int) -> int:
        return _dc._calculate_ttl(base_ttl, settings.L1_CACHE_RANDOM_OFFSET_PERCENT)

    def _calculate_l2_ttl(self, base_ttl: int) -> int:
        return _dc._calculate_ttl(base_ttl, settings.L2_CACHE_RANDOM_OFFSET_PERCENT)

    def _get_raw(self, key: str) -> Any | None:
        value = _dc.l1_get(key)
        if value is not None:
            return value
        value = _dc.l2_get(key)
        if value is not None:
            _dc.l1_set(key, value)
            return value
        return None

    def _set_raw(self, key: str, value: Any, l1_ttl: int | None = None, l2_ttl: int | None = None) -> None:
        final_l1_ttl = l1_ttl if l1_ttl is not None else settings.L1_CACHE_TTL_MEDIUM
        final_l2_ttl = l2_ttl if l2_ttl is not None else settings.L2_CACHE_TTL_MEDIUM
        _dc.l1_set(key, value, ttl=self._calculate_l1_ttl(final_l1_ttl))
        _dc.l2_set(key, value, ttl=self._calculate_l2_ttl(final_l2_ttl))

    def _delete_raw(self, key: str) -> None:
        _dc.l1_delete(key)
        _dc.l2_delete(key)

    def _clear_pattern_raw(self, pattern: str) -> None:
        """[废弃] 按模式物理删除，仅保留供旧接口过渡使用"""
        _dc.l1_clear_pattern(pattern)
        _dc.l2_clear_pattern(pattern)

    # ====================
    # 旧接口（废弃，过渡期保留）
    # ====================

    def get_global(self, resource: str, key: str) -> Any | None:
        """[废弃] 使用 get(cache_type=GLOBAL, ...) 替代"""
        return self.get(
            cache_type=CacheType.GLOBAL,
            resource=resource,
            key=key,
            tags=[f"{resource}:all"],
        )

    def set_global(
        self, resource: str, key: str, value: Any,
        l1_ttl: int | None = None, l2_ttl: int | None = None,
    ) -> None:
        """[废弃] 使用 set(cache_type=GLOBAL, ...) 替代"""
        final_l1_ttl = l1_ttl if l1_ttl is not None else settings.L1_CACHE_TTL_LOW
        final_l2_ttl = l2_ttl if l2_ttl is not None else settings.L2_CACHE_TTL_LOW
        self.set(
            cache_type=CacheType.GLOBAL,
            resource=resource,
            key=key,
            value=value,
            tags=[f"{resource}:all"],
            l1_ttl=final_l1_ttl,
            l2_ttl=final_l2_ttl,
        )

    def delete_global(self, resource: str, key: str) -> None:
        """[废弃] 使用 invalidate(tags=[...]) 替代"""
        self.invalidate([f"{resource}:all"])

    def increment_version(self, namespace: str = "default") -> int:
        """[废弃] 使用 invalidate(tags=[...]) 替代"""
        self.invalidate([f"{namespace}:all"])
        return 0

    def delete(self, cache_type: str, resource: str, key: str = None, tenant_id: int = 0, namespace: str = "default") -> None:
        """[废弃] 使用 invalidate(tags=[...]) 替代"""
        self.invalidate([f"{resource}:all"])

    def delete_by_tenant(self, tenant_id: int) -> None:
        """[废弃] 使用 invalidate(tags=[f"tenant:{tenant_id}"]) 替代"""
        self.invalidate([f"tenant:{tenant_id}"])


cache_manager = CacheManager()
```

### 4.4 CacheKey（修改 `cache_key.py`）

```python
"""缓存键构建器

缓存键格式 (tag_hash 嵌入):
    - GLOBAL: global:{tag_hash}:{resource}:{key}
    - DATA:   data:{tag_hash}:{tenant_id}:{resource}:{key}
    - LIST:   list:{tag_hash}:{tenant_id}:{resource}:{query_hash}
"""

import hashlib
from typing import Any


class CacheType:
    GLOBAL = "global"
    DATA = "data"
    LIST = "list"


def build_cache_key(
    cache_type: str,
    resource: str,
    key: str | None = None,
    query_params: dict[str, Any] | None = None,
    tenant_id: int | None = None,
    tag_hash: str = "v0",
) -> str:
    """构建缓存键

    Args:
        cache_type: 缓存类型 (GLOBAL/DATA/LIST)
        resource: 资源名称
        key: 数据主键 (GLOBAL/DATA 使用)
        query_params: 查询参数 (LIST 使用)
        tenant_id: 租户ID (DATA/LIST 使用)
        tag_hash: 标签版本哈希，由 TagStore.calc_tag_hash 计算
    """
    parts = [cache_type, tag_hash]

    if cache_type == CacheType.GLOBAL:
        parts.append(resource)
        if key:
            parts.append(key)
        return ":".join(parts)

    parts.extend([str(tenant_id or 0), resource])

    if cache_type == CacheType.DATA:
        if key:
            parts.append(key)
        return ":".join(parts)

    if cache_type == CacheType.LIST:
        if query_params:
            sorted_params = sorted(query_params.items())
            query_str = "&".join([f"{k}={v}" for k, v in sorted_params])
            query_hash = hashlib.md5(query_str.encode("utf-8")).hexdigest()[:8]
            parts.append(query_hash)

    full_key = ":".join(parts)

    if len(full_key) > 250:
        key_hash = hashlib.md5(full_key.encode()).hexdigest()
        full_key = f"{cache_type}:{tag_hash}:{resource}:{key_hash}"

    return full_key
```

### 4.5 ORM Events（重写 `orm_events.py`）

```python
"""ORM 事件缓存联动（Tag 机制）

监听 SQLAlchemy Session after_flush 事件，写操作后自动递增相关标签版本。

标签策略:
1. 表级标签: 任何对该模型的写操作都递增 "{entity}:all"
2. 行级标签: 如果能获取实例主键，递增 "{entity}:{id}"
3. 联动标签: 权限相关模型变更时，联动递增 "login_ctx:all"

注意:
- 只注册一次 after_flush，不再有重复监听
- 批量 DELETE/UPDATE 语句不触发 after_flush，业务层需手动调用 invalidate
"""

from sqlalchemy import event
from sqlalchemy.orm import Session

from .tag_store import tag_store


# 模型名 → 表级标签
_CACHE_TABLE_TAG_MAP = {
    "Role": "role:all",
    "Permission": "perm:all",
    "RolePermission": "role_perm:all",
    "RoleSubject": "role_subject:all",
    "DataScopeRule": "data_scope:all",
    "User": "user:all",
    "Member": "member:all",
    "Org": "org:all",
    "OrgSubject": "org_subject:all",
    "DictType": "dict_type:all",
    "DictData": "dict_data:all",
    "SystemConfig": "sys_config:all",
    "Tenant": "tenant:all",
    "AccountBind": "account_bind:all",
}

# 模型名 → 行级标签前缀（主键字段名）
_CACHE_ROW_TAG_MAP = {
    "Role": "id",
    "Permission": "id",
    "User": "id",
    "Tenant": "id",
    "Member": "id",
    "Org": "id",
    "DictType": "id",
    "DictData": "id",
    "SystemConfig": "id",
    "AccountBind": "id",
}

# 权限相关模型，变更时联动清 login_ctx
_LOGIN_CTX_IMPACT_MODELS = {
    "Role", "Permission",
    "RoleSubject", "RolePermission", "DataScopeRule",
    "OrgSubject", "Member", "User",
}


@event.listens_for(Session, 'after_flush')
def after_flush_cache(session, flush_context):
    """写操作后自动递增相关标签版本"""
    tags_to_increment = set()
    need_login_ctx_clear = False

    for instance in list(session.new) + list(session.dirty) + list(session.deleted):
        model_name = type(instance).__name__

        # 表级标签
        table_tag = _CACHE_TABLE_TAG_MAP.get(model_name)
        if table_tag:
            tags_to_increment.add(table_tag)

        # 行级标签
        pk_field = _CACHE_ROW_TAG_MAP.get(model_name)
        if pk_field:
            pk_value = getattr(instance, pk_field, None)
            if pk_value is not None:
                entity_name = model_name.lower()
                tags_to_increment.add(f"{entity_name}:{pk_value}")

        # 联动 login_ctx
        if model_name in _LOGIN_CTX_IMPACT_MODELS:
            need_login_ctx_clear = True

    if need_login_ctx_clear:
        tags_to_increment.add("login_ctx:all")

    if tags_to_increment:
        tag_store.invalidate(list(tags_to_increment))


def register_cache_events() -> None:
    """注册缓存事件监听（导入模块时自动注册，此函数保留兼容性）"""
    pass
```

### 4.6 dogpile_config.py（微调）

需要新增两个底层方法：

```python
def l2_get_int(key: str) -> int:
    """从 Redis 读取整数值"""
    if not _l2_available:
        return 0
    try:
        raw = l2_region.backend.writer_client.get(key)
        if raw is not None:
            return int(raw)
    except Exception:
        pass
    return 0


def l2_incr(key: str) -> int:
    """Redis 原子递增，返回递增后的值"""
    if not _l2_available:
        return 0
    try:
        return l2_region.backend.writer_client.incr(key)
    except Exception:
        return 0
```

保留但标记废弃：
- `l2_get_version` → 被 `l2_get_int` 替代
- `l2_set_version` → 被 `l2_incr` 替代
- `l2_clear_pattern` / `l1_clear_pattern` → 仅废弃接口过渡用

### 4.7 __init__.py 更新

```python
from .cache_key import CacheType, build_cache_key
from .cache_manager import CacheManager, cache_manager
from .tag_store import TagStore, tag_store
from .orm_events import register_cache_events

__all__ = [
    "CacheType",
    "build_cache_key",
    "CacheManager",
    "cache_manager",
    "TagStore",
    "tag_store",
    "register_cache_events",
]
```

### 4.8 删除 `foundation/iam/rbac/cache_events.py`

该文件与 `core/storage/cache/orm_events.py` 重复注册 `after_flush`，直接删除。其功能已由新的统一 ORM 事件完全覆盖。

---

## 五、接口对照表

### 5.1 新接口

| 方法 | 签名 | 说明 |
|------|------|------|
| `get` | `get(cache_type, resource, key=None, tags=None, query_params=None, tenant_id=0)` | 读取缓存 |
| `set` | `set(cache_type, resource, value, key=None, tags=None, query_params=None, tenant_id=0, l1_ttl=None, l2_ttl=None)` | 写入缓存 |
| `invalidate` | `invalidate(tags: list[str])` | 递增标签版本，使关联缓存逻辑失效 |
| `cached` | `cached(resource, tags=None, l1_ttl=None, l2_ttl=None, cache_type=None, tenant_id=0)` | 装饰器 |

### 5.2 旧接口 → 新接口映射

| 旧接口 | 新接口等价调用 | 说明 |
|--------|---------------|------|
| `get_global("role", "1")` | `get(GLOBAL, "role", key="1", tags=["role:all"])` | 自动补充表级标签 |
| `set_global("role", "1", val)` | `set(GLOBAL, "role", val, key="1", tags=["role:all"])` | 自动补充表级标签 |
| `delete_global("role", "1")` | `invalidate(["role:all"])` | 粗粒度失效 |
| `increment_version("role")` | `invalidate(["role:all"])` | 粗粒度失效 |
| `delete(DATA, "role", key="1", tenant_id=1001)` | `invalidate(["role:all"])` | 粗粒度失效 |
| `delete_by_tenant(1001)` | `invalidate(["tenant:1001"])` | 租户级失效 |
| `clear_user_cache(user_id=1)` | `invalidate(["user:1", "user:all"])` | 用户级失效 |

### 5.3 旧接口保留策略

旧接口保留为**废弃包装**，内部调用新接口：
- 功能不 break，现有代码继续运行
- 行为从"版本号递增"变为"表级标签递增"，效果等价（粗粒度失效）
- 业务层后续逐步迁移到新接口，传入更精细的 tags

---

## 六、文件变更清单

| 文件 | 操作 | 变更内容 |
|------|------|---------|
| `src/core/storage/cache/tag_store.py` | **新增** | TagStore 类：标签版本管理 + 哈希计算 |
| `src/core/storage/cache/cache_manager.py` | **重写** | 新接口 + 废弃包装 |
| `src/core/storage/cache/cache_key.py` | **修改** | `version` 参数 → `tag_hash` 参数 |
| `src/core/storage/cache/orm_events.py` | **重写** | 模型→标签映射 + 行级标签 |
| `src/core/storage/cache/dogpile_config.py` | **微调** | 新增 `l2_get_int` / `l2_incr`，旧方法标记废弃 |
| `src/core/storage/cache/__init__.py` | **修改** | 导出 TagStore |
| `src/foundation/iam/rbac/cache_events.py` | **删除** | 消除重复监听 |

---

## 七、迁移指南（本次不执行，供后续参考）

### 7.1 业务层迁移示例

**permission_control.py** 迁移示例：

```python
# 旧代码
RBAC_ROLE_RESOURCE = "rbac:role"
cache_manager.get_global(RBAC_ROLE_RESOURCE, cache_key)
cache_manager.set_global(RBAC_ROLE_RESOURCE, cache_key, role_ids)

# 新代码（行级精准失效）
cache_manager.get(
    cache_type=CacheType.GLOBAL,
    resource="rbac_role",
    key=cache_key,
    tags=[f"role:{rid}" for rid in related_role_ids] + ["role:all"],
)
cache_manager.set(
    cache_type=CacheType.GLOBAL,
    resource="rbac_role",
    key=cache_key,
    value=role_ids,
    tags=[f"role:{rid}" for rid in related_role_ids] + ["role:all"],
)
```

**login_ctx.py** 迁移示例：

```python
# 旧代码
cache_manager.get_global(LOGIN_CTX_RESOURCE, cache_key)
cache_manager.set_global(LOGIN_CTX_RESOURCE, cache_key, ctx_dict)

# 新代码（多标签联动失效）
tags = [f"user:{user_id}"]
if member_id:
    tags.append(f"member:{member_id}")
# 加入用户所有角色的标签
tags.extend([f"role:{rid}" for rid in role_ids])

cache_manager.get(
    cache_type=CacheType.GLOBAL,
    resource="login_ctx",
    key=cache_key,
    tags=tags,
)
```

### 7.2 迁移优先级建议

1. **第一批**：`permission_control.py`、`login_ctx.py`（RBAC 核心路径，收益最大）
2. **第二批**：`tenant/resolver.py`、`uuid_resolver.py`（高频查询路径）
3. **第三批**：`cache_warmup_initializer.py`（预热适配）
4. **第四批**：移除所有废弃包装

---

## 八、后续计划（本次不做）

| 后续任务 | 说明 |
|---------|------|
| 缓存预热重构 | 适配新接口，修正 tag_hash 一致性问题 |
| 业务层迁移 | 按优先级逐步迁移调用方到新接口 |
| 租户标签落地 | 在业务层为租户数据打 `tenant:{id}` 标签 |
| 废弃接口清理 | 所有调用方迁移完成后，移除旧接口 |
| 缓存监控 | 统计命中率、标签版本变更频率等指标 |

---

## 九、风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| Redis 不可用时标签版本降级为 0 | 缓存无法通过 tag 失效，仅靠 TTL 淘汰 | L1 短 TTL（40s）兜底，Redis 恢复后自动恢复 |
| 批量 DELETE 不触发 after_flush | 行级标签无法自动递增 | 表级标签兜底，业务层手动 invalidate |
| 本地缓存 2 秒窗口 | 标签递增后最多 2 秒内可能读到旧版本 | L1 短 TTL（40s）进一步限制窗口，业务可接受 |
| 旧废弃接口行为变化 | `increment_version` 从精确递增变为标签递增 | 效果等价（粗粒度失效），调用方无感知 |
