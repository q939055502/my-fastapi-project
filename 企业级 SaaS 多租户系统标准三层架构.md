# 企业级 SaaS 多租户系统标准三层架构

# 最终版・完整项目目录结构 \+ 逐层级深度分析

这是**企业级 SaaS 多租户系统标准三层架构**，完全适配你的业务，解决了「基础模块与业务模块混淆」「命名冲突」「依赖混乱」所有问题，**现在重构成本最低，后期永不返工**。

我直接给你**可落地、可直接复制**的最终结构，并把**每一层、每一个文件夹的作用、为什么这么放**讲透。

---

## 一、完整最终目录结构

```text
src/
# ==============================================
# 第一层：通用核心层（纯工具 | 无业务 | 无DB依赖）
# 定义：全项目通用底座，任何模块都能引用，它不引用任何业务代码
# ==============================================
├── common/
│   ├── core/                  # 核心框架组件（系统级）
│   │   ├── auth/              # JWT 解析、权限注解、Token工具
│   │   ├── context/           # 请求上下文（租户/用户上下文）
│   │   ├── enums/             # 全局通用枚举（是/否、状态、类型）
│   │   ├── exceptions/        # 全局异常、错误码
│   │   ├── handlers/          # 全局处理器（响应处理器、异常处理器）
│   │   ├── initializers/      # 项目初始化器（数据库、缓存、路由）
│   │   ├── log/               # 日志工具
│   │   ├── middlewares/       # 全局中间件（租户隔离、认证、跨域）
│   │   ├── plugins/           # 第三方插件（OSS、短信、邮件）
│   │   ├── response/          # 统一响应体封装
│   │   ├── scheduler/         # 定时任务
│   │   ├── storage/           # 存储（DB基类、缓存、文件）
│   │   ├── app_setup.py       # 应用初始化入口
│   │   └── config.py          # 全局配置
│   ├── repository/            # 通用仓库基类（BaseRepository）
│   ├── schemas/               # 通用Schema（分页、统一响应）
│   └── utils/                 # 工具函数（时间、加密、字符串、校验）
# ==============================================
# 第二层：基础业务层（核心公共业务 | 全系统依赖）
# 定义：所有业务模块都必须用的基础功能，有DB依赖，无具体业务逻辑
# ==============================================
├── foundation/
│   ├── auth/                  # 认证中心（登录、注册、刷新Token、登出）
│   ├── user/                  # 用户核心（用户信息、密码、个人中心）
│   ├── tenant/                # 租户体系（租户信息、成员、邀请、配额）
│   ├── platform/              # 平台管理（平台管理员、全局RBAC、系统配置）
│   ├── system/                # ✅ 原 modules/common 迁移至此（系统通用业务）
│   │   ├── files/             # 文件上传/下载
│   │   ├── profile/           # 个人信息修改
│   │   ├── account_bind/      # 账号绑定
│   │   └── logs/              # 操作日志
│   ├── rbac/                  # 统一权限中心（角色、菜单、权限）
│   ├── dept/                  # 部门管理
│   └── approval/              # 通用审批（你要做的审批模型）
# ==============================================
# 第三层：数据模型层（数据库ORM模型）
# 定义：纯模型定义，按「平台级/租户级」隔离
# ==============================================
├── models/
│   ├── platform/              # 平台全局模型（无tenant_id）
│   └── tenant/                # 租户私有模型（必须带tenant_id）
# ==============================================
# 第四层：业务模块层（纯业务 | 独立隔离）
# 定义：具体业务功能，只依赖底层，不互相依赖
# ==============================================
├── modules/
│   ├── order/                 # 订单业务（✅ 保留，纯业务）
│   ├── goods/                 # 商品业务（后续新增）
│   ├── payment/               # 支付业务（后续新增）
│   └── report/                # 报表业务（后续新增）
# ==============================================
# 第五层：API路由层（接口统一出口）
# ==============================================
├── api/
│   └── v1/                    # v1版本接口
│       ├── __init__.py        # 统一注册所有路由
│       ├── foundation.py      # 基础业务API
│       └── modules.py         # 业务模块API
# ==============================================
# 项目入口
# ==============================================
└── main.py                    # FastAPI 入口文件
```

---

## 二、核心分层深度分析（最重要！）

### 1\. 第一层：`common/` —— 纯通用工具底座

**定位**：**无业务、无数据库、无业务耦合**
**作用**：给全项目提供底层能力，抽出来可以直接给另一个项目用
**包含**：中间件、异常、日志、工具、基类、配置
**规则**：

- 只被别人依赖，**不依赖任何业务代码**

- 不写任何业务逻辑（比如登录、订单都不能放这）

---

### 2\. 第二层：`foundation/` —— 基础业务核心（解决你最大的困惑）

**定位**：**全系统必须依赖的公共业务**
**这一层放的是：登录、用户、租户、权限、部门、审批、系统通用功能**
**解决的问题**：

- 原来 `modules/` 下的 `auth/tenant/platform/common` 都是基础业务，和订单混放太乱

- 原 `modules/common` → 改名迁移到 `foundation/system`，**彻底避免命名冲突**

**所有业务模块（订单 / 商品）都必须依赖这一层，但这一层不依赖业务模块**

---

### 3\. 第三层：`models/` —— 数据模型

**定位**：纯数据库结构，按「平台 / 租户」物理隔离

- `platform/`：全局数据，无租户隔离

- `tenant/`：租户数据，强制带 `tenant_id`

---

### 4\. 第四层：`modules/` —— 纯业务模块

**定位**：**具体业务功能，完全独立**
**规则**：

- 只放具体业务（订单、商品、支付\.\.\.）

- 业务模块之间**互不依赖**（订单不引用商品，商品不引用支付）

- 只依赖 `common/` 和 `foundation/`

---

### 5\. 第五层：`api/` —— 接口统一出口

**定位**：所有接口的路由注册，不写业务逻辑

- 统一管理接口版本

- 按「基础业务 / 业务模块」拆分接口

---

## 三、关键迁移对应（你原来的代码 → 新位置）

|原来的位置|迁移后的新位置|原因|
|---|---|---|
|`modules/auth`|`foundation/auth`|认证是基础业务|
|`modules/tenant`|`foundation/tenant`|租户是核心基础|
|`modules/platform`|`foundation/platform`|平台管理是基础|
|`modules/common`|`foundation/system`|系统通用业务，避免命名冲突|
|`modules/order`|`modules/order`|纯业务，保留不动|

---

## 四、这个架构的 5 大核心优势（为什么必须这么改）

### 1\. 边界绝对清晰

- 工具 → `common`

- 基础业务 → `foundation`

- 纯业务 → `modules`
新人接手 1 分钟看懂结构

### 2\. 依赖关系绝对安全（永不循环依赖）

**单向依赖规则**：

```Plain Text
业务模块(modules) → 基础业务(foundation) → 通用核心(common)
```

下层永远不依赖上层，彻底解决循环依赖问题

### 3\. 命名冲突彻底解决

- `src/common` = 纯工具

- `foundation/system` = 系统通用业务
再也不会有两个 `common` 混淆

### 4\. 重构成本极低

你只需要：

1. 新建 `foundation` 文件夹

2. 把 `modules` 下的基础模块移过去

3. IDE 一键批量替换导入路径
**1 小时内完成重构，不修改任何业务逻辑**

### 5\. 无限扩展

- 加通用工具 → 放 `common`

- 加基础功能（如消息、公告）→ 放 `foundation`

- 加业务（如商品、支付）→ 放 `modules`
后期加 100 个模块都不会乱

---

## 五、一句话总结

**`common`**** 做工具，****`foundation`**** 做底座，****`modules`**** 做业务**
这是国内大厂 SaaS 系统的**标准架构**，你现在按这个结构重构，就是一步到位，后期永远不用再动架构。

> （注：文档部分内容可能由 AI 生成）
